# AGENTS.md

이 저장소에서 작업하는 모든 AI 코딩 에이전트가 따를 지침. 도구에 관계없이 적용된다.

- 기능 명세 → `PRD.md`
- 환경 설정·실행 → `DEVELOPMENT.md`
- Claude Code 전용 세부 규칙 → `CLAUDE.md`

---

## 프로젝트 개요

**온코케어(OncoCare)** — 공공데이터를 결합해 6대암 국가검진 예산 배분을 진단하고, 개입 기록으로 다음 해 효과를 검증하는 B2G 웹 서비스.

```
공공데이터 5종 → 파이프라인(S1~S6) → 세그먼트 89,299행
                                         ↓
                    분석(회귀·kNN·층화) → 사전 계산 산출물
                                         ↓
                    FastAPI(조회 6·생성 2·기록 1) → HTML/CSS/JS 화면 9종
```

---

## 개발 환경 설정

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env          # GEMINI_API_KEY 입력
```

원본 데이터는 `data/raw/`에 배치한다. 파일명을 변경하지 않는다(파이프라인이 파일명으로 분기).

---

## 실행 명령

```bash
# 파이프라인 — 단계별 실행 권장
python pipeline/run.py --stage s1
python pipeline/run.py --stage s2
python pipeline/run.py --stage all

# 분석 산출물 생성
python analysis/build_artifacts.py

# API
uvicorn api.main:app --reload --port 8000

# 프론트엔드 (정적)
python -m http.server 5173 --directory web

# 테스트
pytest -q
pytest tests/test_no_llm_in_query.py -v     # 조회 API의 LLM 미사용 검증
```

---

## 절대 규칙

에이전트는 아래를 위반하는 코드를 작성하지 않는다. 위반이 발견되면 되돌린다.

### 1. 조회 라우터는 LLM을 호출하지 않는다

`api/routers/query.py`에서 `api/llm.py`를 import 하지 않는다. 화면에 표시되는 모든 수치는 `data/processed/`의 사전 계산 결과에서만 온다.

**이유** — 담당자가 화면 문장을 예산요구서에 옮겨 적는다. 같은 요청에 항상 같은 값이 반환되어야 한다.

`tests/test_no_llm_in_query.py`로 강제한다.

### 2. 개인 식별정보를 저장하지 않는다

| 금지 | 허용 |
|---|---|
| 이름 · 주민번호 · 전화번호 · 주소 | 세그먼트 ID |
| 개인 수검 이력 | 배정군(연락/대기) · 인원 수 |
| 개인 명단 | 사유 응답 집계 · 발송 일자 |

실행 기록도 세그먼트 단위 집계로만 저장한다. 개인 추적 기능을 만들지 않는다.

### 3. 수치를 하드코딩하지 않는다

```python
# 금지
LUNG_ZERO_COUNT = 101
EXPECTED_R2 = 0.878

# 허용
lung_zero = int((inst["기관_폐암"] == 0).sum())
r2 = r2_score(y_true, y_pred)
```

문서에 적힌 수치는 모두 데이터에서 재산출한다. 값이 문서와 다르면 문서를 고치지 말고 원인을 확인하고 사용자에게 보고한다.

### 4. LLM이 숫자를 생성하지 않는다

생성 엔드포인트에서 수치는 조회 결과를 프롬프트에 명시하고 그 값만 인용하게 한다. 생성 후 프롬프트에 없는 숫자가 출력에 있으면 재생성(최대 2회)하고, 실패 시 오류를 반환한다.

### 5. 무작위 분할 검증을 쓰지 않는다

```python
tr = df[df["연도"] == 2023]
te = df[df["연도"] == 2024]
```

`train_test_split`을 사용하지 않는다. 동일 연도 코호트가 학습·검증에 섞이면 낙관적 편향이 발생한다.

### 6. 전년도 수검률을 기대치 모델에 넣지 않는다

| 모델 | 피처 | 용도 |
|---|---|---|
| A안 (기본) | 여건 15개 | 개입 우선순위 |
| B안 (별도) | A안 + 전년수검률 | 급변 탐지 전용 |

B안을 우선순위 화면에 쓰면 잔차가 「여건 대비 격차」가 아니라 「전년 대비 변화」가 된다.

### 7. 화면 표현 규칙

| 금지 | 사용 |
|---|---|
| 목표 51.0% | 여건 유사 20곳 평균 51.0% |
| 9.1%p 미달 | 9.1%p 차이 |
| AI가 예측한 값 | 여건이 비슷한 20곳이 실제로 낸 평균 |
| 수검률이 오릅니다 | 회복 여지 1,034명 |

모든 수검률 표시에 분모를 병기한다 — 「건강보험 가입자 기준」

「이 값은 평가가 아니라 검토 시작점입니다」 문구를 화면에 고정 배치한다.

---

## 기술 스택 — 변경 금지

| 계층 | 기술 |
|---|---|
| 파이프라인·분석 | Python 3.12 · pandas · scikit-learn |
| 모델 | HistGradientBoostingRegressor (LightGBM 미사용) |
| 백엔드 | FastAPI + uvicorn |
| 프론트엔드 | HTML / CSS / Vanilla JS (프레임워크 없음) |
| LLM | Gemini API (생성 엔드포인트에서만) |
| 저장 | parquet (분석) · SQLite (실행 기록) |

Streamlit을 쓰지 않는다. 화면은 HTML/CSS/JS로 직접 작성하며 API 연동이 처음부터 포함되어야 한다.

---

## 코드 스타일

```
· Python 3.12 · 타입 힌트 사용 · 함수당 한 가지 책임
· 컬럼명·변수명은 한글 유지 (데이터 원본이 한글이며 문서와 대조 가능해야 함)
· 파이프라인 각 단계는 독립 실행 가능하고 중간 산출물을 남긴다
· 매직 넘버는 모듈 상단 상수로 (단, 검증된 분석 수치는 상수화하지 말고 재산출)
· API 응답은 Pydantic 모델로 정의 (api/schemas.py)
· 주석은 "무엇"이 아니라 "왜" — 특히 데이터 결함 대응 코드
```

**한글 컬럼명 예시**

```python
FEATURES = ["연령n", "여자", "암_위암", ..., "기관밀도", "관내0"]
df["수검률"] = df["수검자"] / df["대상자"] * 100
```

---

## 테스트

```bash
pytest -q                                    # 전체
pytest tests/test_pipeline.py -v             # 파이프라인 검증
pytest tests/test_no_llm_in_query.py -v      # 조회 API LLM 미사용
pytest tests/test_api.py -v                  # 엔드포인트 응답
```

### 필수 테스트

| 파일 | 검증 |
|---|---|
| `test_pipeline.py` | 행수 89,299 ± 100 · 매칭률 · 수검률 0~100 범위 |
| `test_no_llm_in_query.py` | `query.py` 소스에 `llm` 참조 0건 |
| `test_api.py` | 동일 요청 10회 반복 시 응답 동일 (결정성) |
| `test_records_schema.py` | 실행 기록 테이블에 개인정보 컬럼 0개 |

새 기능을 추가할 때 해당 테스트를 먼저 작성한다.

---

## 커밋 규칙

```
한 커밋에 한 가지 변경. 파이프라인은 단계별로 분리.

data/ 하위는 .gitignore — 원본 데이터를 커밋하지 않는다
.env는 .gitignore — .env.example만 커밋

커밋 메시지 예시
  pipeline: S2 시도명 표준화 및 후행 공백 제거
  analysis: 여건 기대치 회귀 모델 (HistGB R² 0.878)
  api: 조회 엔드포인트 summary·priority 추가
  web: 화면 [0] 우리 구 현황 구현
  fix: 검진기관 매칭률 44% → 99.8% (공백 제거 누락)
```

---

## 작업 순서

에이전트는 이 순서를 지킨다. 앞 단계 산출물 없이 뒷 단계를 시작하지 않는다.

```
1. pipeline/     S1~S6 → segments.parquet 생성 및 검증
2. analysis/     기대치·유사지역·층화 → 사전 계산 산출물 9종
3. api/          조회 엔드포인트 6종 (LLM 없이)
4. web/          화면 [0][1][2] (조회만으로 동작)
5. api/          생성 엔드포인트 2종 (LLM)
6. web/          화면 [3]~[7]
7. api/web       [8] 실행 기록
```

**한 번에 전체를 만들지 않는다.** 각 단계 후 검증 로그를 출력하고 사용자 확인을 받는다.

---

## 하지 않는 것

```
❌ 개인 위험도 예측 — 의료기기(SaMD) 규제 대상
❌ 검진 대상 기준 변경·신설
❌ 개인정보 수집·저장·명단 생성
❌ 「온코」 대화 기능 — 화면 디자인까지만, 기능은 2차 개발
❌ 실시간 데이터 연동 — 공공데이터 갱신 주기가 연 단위
❌ 개입 효과(uplift) 산출 — 개입 기록 부재. 파일럿 후 2단계
❌ 모바일 반응형 — 데스크톱 1920×1080 전용
```

---

## 브랜드 토큰 — `web/css/tokens.css`

```css
:root {
  --onco-navy:   #1F4E78;   /* 주색 — 제목·표 머리글·주요 버튼 */
  --care-teal:   #008080;   /* 보조색 — 강조 수치·요인 그래프 */
  --alert-coral: #E05A47;   /* 강조색 — 음수 격차 · 화면당 3개 이내 */
  --bg-light:    #F2F4F7;   /* 배경 — 카드·사이드바·구분선 */
  --ink:         #222222;   /* 본문 */
}
```

Alert Coral은 화면당 3개를 넘지 않는다. 색만으로 의미를 전달하지 않고 숫자와 방향 기호를 함께 표기한다.

---

## 데이터 결합 키

```python
SIDO_MAP = {"강원도": "강원특별자치도", "전라북도": "전북특별자치도"}

def norm_sido(s: str) -> str:
    s = str(s).strip()          # 후행 공백 필수 — 미처리 시 매칭률 44%
    return SIDO_MAP.get(s, s)

df["키"] = df["시도"] + "|" + df["시군구"]

# 일반구 → 시 단위 축약 (검진기관·소득 데이터가 시 단위)
df["키_시"] = df["키"].str.replace(
    r"부천시 (소사|오정|원미)구", "부천시", regex=True)
df["키_시"] = df["키_시"].replace({"세종특별자치시|세종시": "세종특별자치시|"})
```

**매칭률 기준** — 검진기관 ≥ 99% · 소득 ≥ 85% · 적용인구 ≥ 99%. 미달 시 전처리 오류다.

---

## 우선순위 필터 — 항상 적용

```python
MIN_RECOVERY = 300      # 회복 여지 300명 미만 제외
MAX_AGE_IDX = 11        # 연령n < 11 (75세 미만)
```

85세 이상 세그먼트가 최하위를 독식하나 고령에서는 검진 이득이 감소하고 과잉진단 우려가 있어 개입 대상에서 제외한다.
