# MODEL_VALIDATION_PLAN.md — 모델 성능 검증 계획서

> 온코케어 예측·해석 모델 4종에 대한 성능 검증 작업 계획. 작성 근거는 실제 데이터(`data/processed/segments.parquet`)로 재현한 사전 진단 결과이며, 수치는 재실행 시 재검증한다(CLAUDE.md 절대 규칙 3).
>
> 관련 문서 — `CLAUDE.md`(개발 규칙) · `DEVELOPMENT.md`(검증 게이트·트러블슈팅) · `PRD.md`(수용 기준)

---

## 1. 배경 및 목적

온코케어는 4개의 사전 계산 모델을 화면에 그대로 인용한다. **조회 API는 LLM을 호출하지 않으므로**(CLAUDE.md 절대 규칙 1) 화면에 표시되는 모든 수치의 신뢰성은 전적으로 이 모델들의 정확도에 달려 있다. 담당자가 화면 문장을 예산요구서에 옮겨 적는 서비스 특성상, "기준은 통과했다"는 결과만으로는 부족하고 **어떤 세그먼트에서 왜 틀리는지**까지 검증해야 한다.

본 계획서는 2026-08-19 사전 진단에서 발견된 이슈를 근거로, 정식 검증 작업의 범위·방법·합격기준·일정을 정의한다.

---

## 2. 검증 대상 모델 4종

| 모델 | 파일 | 알고리즘 | 용도 | 기존 수용 기준 |
|---|---|---|---|---|
| 기대치 회귀 (A안) | `analysis/expectation.py` | HistGradientBoostingRegressor | 개입 우선순위 산출용 잔차·회복여지 | R² ≥ 0.85 · MAE ≤ 4.5%p |
| 계수 해석 | `analysis/coefficients.py` | LinearRegression | 화면 문구용 계수 해석("관내 기관 부재 시 -X%p") | 정의 없음 — **본 계획서에서 신설** |
| 유사 지역 kNN | `analysis/similar.py` | NearestNeighbors(k=21) | 여건 유사 20곳 평균 산출 | 도농 일치율 ≥ 95% · 평균 SE ≤ 2.0%p |
| 층화 교차표 | `analysis/strata.py` | 집계(비모델) | 도농×소득 9개 층 비교 | 정의 없음 — **본 계획서에서 신설** |

---

## 3. 사전 진단 결과 요약 (2026-08-19 실측)

`data/processed/segments.parquet` 기준 A안 재현: 학습 2023년(18,851행) · 검증 2024년(18,896행).

| 항목 | 결과 | 판정 |
|---|---|---|
| Test R² | 0.8809 | 기준(≥0.85) 통과 |
| Test MAE | 4.29%p | 기준(≤4.5%p) 통과, 여유 0.21%p |
| Train R² vs Test R² gap | 0.0393 | Train 내부 5-Fold CV(mean 0.8936, std 0.0055)와 근접 — 심각한 과적합 아님 |
| Train/Test 피처 분포 시프트 | 편차 <0.03 (전 피처) | 없음 |

**발견된 리스크 2건**

1. **폐암 잔차 표준편차 14.24** — 다른 5개 암종(3.9~7.7)의 2~4배. 극단 잔차 상위 10건 중 8건이 폐암·간암의 85세 이상 소표본 세그먼트(수검률 100% vs 예측 33~52%)에서 발생. `MIN_RECOVERY=300`, `연령n<11` 필터는 우선순위 산출 단계에만 적용되고 **모델 학습 자체에는 적용되지 않아**, 이 노이즈가 학습에 그대로 반영됐을 가능성.
2. **`관내0`의 permutation importance가 사실상 0(-0.0000)** — `coefficients.py`의 선형회귀는 이 변수를 정책 서사의 핵심으로 쓰는데("관내 기관 부재 시 -X%p"), HGB 예측 모델에서는 기여도가 최하위다. `기관밀도`/`기관전체밀도`와의 다중공선성이 원인으로 추정되나, 두 모델이 같은 변수를 다르게 취급하는 점이 화면에 병기될 때 설명 없이 노출되면 신뢰성 문제가 된다.

기타 — 연령대가 높을수록(20대 std 4.1 → 70대 std 7.5), 군(郡) 지역일수록(std 7.31 > 구 5.84), 관내0=1 세그먼트일수록(std 11.83 vs 6.00) 잔차가 커진다.

---

## 4. 검증 작업 계획

### Phase 1 — 자동화된 성능 게이트 (pytest)

현재 `tests/`에는 파이프라인 구조·행수 검증(`test_pipeline.py`)만 있고, **모델 성능 자체를 매 실행마다 검증하는 테스트가 없다.** `analysis/expectation.py`가 콘솔에 R²/MAE를 출력하지만 실패해도 프로세스가 멈추지 않는다.

| 항목 | 내용 |
|---|---|
| 산출물 | `tests/test_model_performance.py` |
| 방법 | `expectation.parquet`을 로드해 저장된 기대치·잔차로 R²·MAE 재계산, 하한선 assert |
| 합격 기준 | R² ≥ 0.85, MAE ≤ 4.5%p, `abs(train_r2 - test_r2) ≤ 0.06`(과적합 상한 신설) |
| 추가 게이트 | `similar.py`의 도농 일치율 ≥ 95%, 평균 SE ≤ 2.0%p 를 코드 assert로 승격(현재는 print 경고만 존재) |
| 소요 | 0.5일 |

### Phase 2 — 폐암/소표본 잔차 노이즈 원인 조사

| 항목 | 내용 |
|---|---|
| 목적 | 폐암 잔차 std 14.24의 원인이 ⓐ 소표본(85세+ 등) 노이즈인지 ⓑ 폐암 자체의 구조적 이질성(수검 대상 정의·흡연력 기반 선별 등)인지 분리 |
| 방법 | ① `연령n<11` 필터를 학습 데이터에도 적용한 재학습 vs 미적용 비교, ② 폐암만 별도 세그먼트로 residual by 대상자 규모(quantile) 분석 |
| 합격 기준 | 필터 적용 후 폐암 잔차 std가 다른 암종 평균(≈4.8)의 2배 이내로 축소되는지 확인. 축소되지 않으면 "폐암은 구조적으로 이질적"이라는 결론을 CLAUDE.md/PRD.md에 명문화 |
| 산출물 | 조사 결과 메모(`docs/` 또는 `DEVELOPMENT.md` 트러블슈팅 표 갱신) |
| 소요 | 1일 |

### Phase 3 — `관내0` 선형·HGB 불일치 조사

| 항목 | 내용 |
|---|---|
| 목적 | 화면에 "관내 기관 부재 시 -X%p"(선형 계수)와 우선순위 순위(HGB 잔차)를 함께 쓸 때 모순되게 보이지 않는지 확인 |
| 방법 | ① `관내0`·`기관밀도`·`기관전체밀도` 간 VIF(분산팽창지수) 계산, ② `관내0` 단독으로 뺀 HGB 재학습 시 R² 변화량 측정(사실상 불필요한 변수인지) |
| 합격 기준 | VIF ≥ 5(다중공선성 확인) 시 화면 문구에 "이 두 지표는 서로 다른 관점(단독 효과 vs 예측 기여도)"이라는 각주 추가를 권고안으로 제출 |
| 산출물 | 조사 결과 메모 + 필요시 화면 문구 수정 제안 (web/factors.html 등) |
| 소요 | 0.5일 |

### Phase 4 — 계수·kNN 안정성 검증

| 항목 | 내용 |
|---|---|
| 목적 | `coefficients.py`(선형 계수)와 `similar.py`(유사 20곳)가 연도 변경에도 안정적인지 확인 — 계수가 매년 크게 흔들리면 화면 문구의 신뢰도 문제 |
| 방법 | 2023 vs 2024 학습 데이터로 각각 계수·유사지역 산출 후 비교(부트스트랩 대신 실제 연도 비교 — CLAUDE.md 절대 규칙 5 준수, `train_test_split` 미사용) |
| 합격 기준 | 주요 계수(관내0·소득로그) 부호 유지 및 크기 변동 ≤ 30%, 유사 20곳 중 연도 간 교체율 ≤ 20% |
| 소요 | 1일 |

---

## 5. 일정

| 일 | 작업 | 산출물 |
|---|---|---|
| 1 | Phase 1 — 성능 게이트 테스트 | `tests/test_model_performance.py` |
| 2 | Phase 2 — 폐암 잔차 원인 조사 | 조사 메모 + (필요시) 학습 필터 반영 |
| 2 (병행) | Phase 3 — 관내0 불일치 조사 | 조사 메모 |
| 3 | Phase 4 — 계수·kNN 연도 안정성 | 조사 메모 |
| 3 | 종합 — DEVELOPMENT.md 트러블슈팅 표·PRD.md 수용 기준 갱신 | 문서 갱신 |

총 3일 소요.

---

## 6. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 폐암 잔차가 필터링 후에도 줄지 않음 | 우선순위 산출 시 폐암을 별도 신뢰도 배지("소표본 주의")로 표시 — 모델 재설계 대신 화면 표기로 대응 |
| `관내0` 제거 시 R² 유의하게 하락 | 변수 제거 대신 화면 각주로만 대응(4번 항목 유지) |
| Phase 4에서 연도 간 계수 변동이 큼 | 계수 기반 문구를 "최근 1개년 계수" 대신 "최근 2개년 평균 계수"로 완화 — 하드코딩 금지 원칙(절대 규칙 3) 유지하며 재산출 로직만 변경 |
| 검증 중 새로운 데이터 결함 발견 | DEVELOPMENT.md 4절(알려진 데이터 결함 6건) 목록에 7번째 항목으로 추가 |

---

## 7. 완료 기준 (Definition of Done)

```
□ tests/test_model_performance.py 가 pytest -q 통과
□ similar.py 도농 일치율·SE 게이트가 assert로 승격 (print 경고 아님)
□ 폐암 잔차 원인 조사 결론이 DEVELOPMENT.md 트러블슈팅 표에 반영
□ 관내0 다중공선성 조사 결론이 문서화 (VIF 수치 포함)
□ 계수·kNN 연도 안정성 결과가 PRD.md 수용 기준에 반영
```
