# CLAUDE.md — 온코케어(OncoCare) 개발 지침

> Claude Code가 이 저장소에서 작업할 때 항상 따를 규칙.
>
> | 문서 | 내용 |
> |---|---|
> | `AGENTS.md` | AI 에이전트 공통 지침 (도구 중립) |
> | **`CLAUDE.md`** | **Claude Code 전용 — 이 문서** |
> | `DEVELOPMENT.md` | 환경 설정 · 실행 명령 · 트러블슈팅 |
> | `PRD.md` | 화면 9종 · API 명세 · 수용 기준 |

---

## 무엇을 만드는가

공공데이터를 결합해 **6대암 국가검진 예산이 어디로 가야 하는지 진단**하고, 그 판단을 현장에서 실행할 수 있게 하며, 실행 기록으로 다음 해 효과를 검증하는 B2G 웹 서비스.

```
데이터 파이프라인  →  분석(회귀·kNN·층화)  →  사전 계산 산출물
                                              ↓
                     FastAPI (조회 6 + 생성 2 + 기록 1)
                                              ↓
                     HTML/CSS/JS 화면 9종
```

---

## 기술 스택 — 변경 금지

| 계층 | 기술 | 비고 |
|---|---|---|
| 파이프라인·분석 | Python 3.12 · pandas · scikit-learn | LightGBM 미사용 |
| 모델 | **HistGradientBoostingRegressor** | sklearn 기본 포함 |
| 백엔드 | **FastAPI** + uvicorn | |
| 프론트엔드 | **HTML / CSS / Vanilla JS** | 프레임워크 없음 |
| LLM | **Gemini API** | 생성 엔드포인트에서만 |
| 저장 | parquet (분석 산출물) · SQLite (실행 기록) | |
| 배포 | Railway (백엔드) · 정적 호스팅 (프론트) | |

**Streamlit을 쓰지 않는다.** 화면은 HTML/CSS/JS로 직접 작성하며 **API 연동이 처음부터 포함되어야 한다.** Stitch 등 AI 화면 생성 도구는 디자인 참고용으로만 사용하고, 산출물을 그대로 가져다 쓰지 않는다(수치가 박힌 정적 HTML이라 개조 비용이 크다).

---

## 디렉터리 구조

```
oncocare/
├── CLAUDE.md
├── PRD.md
├── requirements.txt
├── .env.example              # GEMINI_API_KEY 등
├── data/
│   ├── raw/                  # 공공데이터 원본 · git 제외
│   ├── interim/              # 단계별 중간 산출물 · git 제외
│   └── processed/            # segments.parquet 등 · git 제외
├── pipeline/
│   ├── s1_collect.py         # 원천 로드
│   ├── s2_clean.py           # 표준화·키 생성
│   ├── s3_merge.py           # 결합
│   ├── s4_expand.py          # wide → long
│   ├── s5_features.py        # 파생 피처
│   ├── s6_split.py           # 시간 분할
│   └── run.py                # --stage all
├── analysis/
│   ├── expectation.py        # 여건 기대치 회귀
│   ├── similar.py            # kNN 유사 지역 20곳
│   ├── strata.py             # 도농×소득 9개 층
│   ├── coefficients.py       # 선형회귀 계수 (해석용)
│   └── build_artifacts.py    # 화면용 사전 계산 → parquet/json
├── api/
│   ├── main.py
│   ├── deps.py               # 산출물 로더 (기동 시 1회)
│   ├── schemas.py            # Pydantic 모델
│   ├── routers/
│   │   ├── query.py          # 조회 — LLM 호출 금지
│   │   ├── generate.py       # 생성 — LLM 사용
│   │   └── records.py        # 실행 기록
│   └── llm.py                # Gemini 래퍼
├── web/
│   ├── index.html
│   ├── css/tokens.css        # 브랜드 색상 변수
│   ├── css/app.css
│   ├── js/api.js             # fetch 래퍼
│   ├── js/router.js
│   └── pages/s0.js ~ s8.js   # 화면 9종
└── tests/
    ├── test_pipeline.py
    ├── test_api.py
    └── test_no_llm_in_query.py   # 조회 API의 LLM 미사용 검증
```

---

## 절대 규칙 — 위반 시 되돌린다

### 1. 조회 API는 LLM을 호출하지 않는다

```python
# api/routers/query.py 에서는 llm 모듈을 import 하지 않는다
# 화면에 표시되는 모든 수치는 사전 계산 결과에서만 나온다
```

**이유** — 담당자가 화면 문장을 예산요구서에 옮겨 적는다. 같은 요청에 항상 같은 값이 반환되어야 한다.

`tests/test_no_llm_in_query.py`로 강제한다. `query.py`가 `llm`을 참조하면 테스트 실패.

### 2. 개인 식별정보를 저장하지 않는다

```
저장 금지   이름 · 주민번호 · 전화번호 · 주소 · 개인 수검 이력
저장 허용   세그먼트 ID · 배정군(연락/대기) · 연락 여부 · 사유 응답 코드 · 발송 일자
```

**실행 기록([8] 화면)도 세그먼트 단위 집계로만 저장한다.** 개인 추적 기능을 만들지 않는다.

### 3. 검증된 수치를 코드에 하드코딩하지 않는다

```python
# 금지
LUNG_ZERO_COUNT = 101

# 허용
lung_zero = (inst["기관_폐암"] == 0).sum()
```

**문서에 있는 수치(R² 0.878, 101곳, 33.4%p 등)는 모두 데이터에서 재산출한다.** 값이 달라지면 문서를 고치는 것이 아니라 원인을 확인한다.

### 4. LLM이 숫자를 만들지 않는다

생성 엔드포인트에서 LLM을 호출할 때, 수치는 **조회 결과를 프롬프트에 넣어 인용하게 한다.**

```python
# 프롬프트에 수치를 명시하고, 그 값만 사용하도록 지시
prompt = f"""
아래 수치만 사용하십시오. 새로운 숫자를 만들지 마십시오.
- 우리 구 수검률: {rate}%
- 여건 유사 20곳 평균: {similar}%
...
"""
```

생성 후 **수치 검증**을 수행한다. 프롬프트에 없는 숫자가 출력에 있으면 재생성하거나 폐기한다.

### 5. 무작위 분할 검증만 사용한다

```python
tr = df[df["연도"] == 2023]   # 학습
te = df[df["연도"] == 2024]   # 검증
```

`train_test_split`을 사용하지 않는다. 동일 연도 코호트가 학습·검증에 함께 들어가 낙관적 편향이 발생한다.

### 6. 전년도 수검률을 기대치 모델에 넣지 않는다

```
A안 (기본)   여건 15피처                → 개입 우선순위 산출용
B안 (별도)   A안 + 전년도 수검률 (16피처)  → [9] 급변 탐지 전용
```

B안을 우선순위 화면에 쓰면 잔차가 「여건 대비 격차」가 아니라 「전년 대비 변화」가 된다.

### 7. 화면 표현 규칙

| 금지 | 사용 |
|---|---|
| 목표 51.0% | **여건 유사 20곳 평균 51.0%** |
| 9.1%p 미달 | **9.1%p 차이** |
| AI가 예측한 값 | **여건이 비슷한 20곳이 실제로 낸 평균** |
| 수검률이 오릅니다 | **회복 여지 1,034명** |
| 수검률이 많이 낮습니다 | **여건 대비 −9.1%p** |

**모든 수검률 표시에 분모를 병기한다** — 「건강보험 가입자 기준」

**「이 값은 평가가 아니라 검토 시작점입니다」 문구를 화면에 고정 배치한다.**

---

## 브랜드 토큰 — `web/css/tokens.css`

```css
:root {
  --onco-navy:  #1F4E78;   /* 주색 — 제목·표 머리글·주요 버튼 */
  --care-teal:  #008080;   /* 보조색 — 강조 수치·요인 그래프 */
  --alert-coral:#E05A47;   /* 강조색 — 음수 격차 · 화면당 3개 이내 */
  --bg-light:   #F2F4F7;   /* 배경 — 카드·사이드바·구분선 */
  --ink:        #222222;   /* 본문 */
}
```

**Alert Coral은 화면당 3개를 넘지 않는다.** 모든 것이 빨갛게 보이면 무엇이 급한지 알 수 없다.

**색만으로 의미를 전달하지 않는다.** 격차는 숫자(−9.1%p)와 방향 기호를 함께 표기한다.

---

## 데이터 결합 키 — `pipeline/s2_clean.py`

```python
SIDO_MAP = {"강원도": "강원특별자치도", "전라북도": "전북특별자치도"}

def norm_sido(s: str) -> str:
    s = str(s).strip()          # 후행 공백 필수 (미처리 시 매칭률 44%)
    return SIDO_MAP.get(s, s)

df["키"] = df["시도"] + "|" + df["시군구"]

# 일반구 → 시 단위 축약 (검진기관·소득 데이터가 시 단위)
df["키_시"] = df["키"].str.replace(
    r"부천시 (소사|오정|원미)구", "부천시", regex=True)
df["키_시"] = df["키_시"].replace({"세종특별자치시|세종시": "세종특별자치시|"})
```

**매칭률 기준** — 검진기관 99.8% · 소득 86.3% · 적용인구 99.3%. 이보다 낮으면 전처리 오류다.

---

## 세그먼트 스키마 — `data/processed/segments.parquet`

| 컬럼 | 형 | 설명 |
|---|---|---|
| 연도 | int | 2020 · 2022 · 2023 · 2024 |
| 시도 · 시군구 · 키 | str | 표준화된 행정구역 |
| 성별 | str | 남자 / 여자 |
| 연령 | str | 20~24 … 85이상 (14구간) |
| 암종 | str | 위암·대장암·간암·유방암·자궁경부암·폐암 |
| 대상자 · 수검자 · 미수검 | float | 원천 값 |
| **수검률** | float | 수검자 ÷ 대상자 × 100 — **타깃 Y** |
| 소득로그 · 인구로그 | float | 로그 변환 |
| 기관수 · 기관밀도 · 관내0 · 기관전체밀도 | float | 접근성 |
| 군 · 구 · 연령n · 여자 · 암_* | int | 파생·인코딩 |
| 전년수검률 | float | 시차변수 — **B안 전용** |

**행 수 89,299.** 대상자 0 조합(남성의 유방암 등)은 정의상 없는 것이므로 제외한다. 결측이 아니다.

---

## 필터 — 우선순위 산출 시 항상 적용

```python
MIN_RECOVERY = 300      # 회복 여지 300명 미만 제외
MAX_AGE_IDX = 11        # 연령n < 11 (75세 미만)
```

**85세 이상 세그먼트가 부합률 최하위를 독식**하나(자궁경부암 여성 6.1%) 고령에서는 검진 이득이 감소하고 과잉진단 우려가 있어 개입 대상에서 제외한다.

---

## 커밋·작업 규칙

- 한 커밋에 한 가지 변경. 파이프라인 단계별로 커밋 분리.
- `data/` 하위는 `.gitignore`. 원본 데이터를 커밋하지 않는다.
- API 키는 `.env`. `.env.example`만 커밋한다.
- 파이프라인 수정 시 검증 로그(매칭률·행수·범위)를 출력하고 결과를 확인한 뒤 다음 단계로 넘어간다.
- 모델 파라미터 변경 시 `random_state=42`를 유지하고 성능 변화를 기록한다.

---

## 개발 순서 — 이 순서를 지킨다

```
1. pipeline/     S1~S6 → segments.parquet 생성 및 검증
2. analysis/     기대치·유사지역·층화 → 사전 계산 산출물
3. api/          조회 엔드포인트 6종 (LLM 없이)
4. web/          화면 [0][1][2] (조회만으로 동작)
5. api/          생성 엔드포인트 2종 (LLM)
6. web/          화면 [3]~[7]
7. api/web       [8] 실행 기록
```

**2단계 산출물 없이 3단계를 시작하지 않는다.** API는 사전 계산 결과를 읽기만 한다.

---

## 하지 않는 것

```
❌ 개인 위험도 예측 — 의료기기(SaMD) 규제 대상
❌ 검진 대상 기준 변경·신설
❌ 개인정보 수집·저장
❌ 「온코」 대화 기능 — 화면 디자인까지만, 기능은 2차 개발
❌ 실시간 데이터 연동 — 공공데이터 갱신 주기가 연 단위
❌ 개입 효과(uplift) 산출 — 개입 기록 부재. 파일럿 후 2단계
```

---

## 참조 문서

| 문서 | 내용 |
|---|---|
| `AGENTS.md` | AI 에이전트 공통 지침 (도구 중립) |
| `DEVELOPMENT.md` | 환경 설정 · 실행 명령 · 데이터 결함 6건 · 트러블슈팅 |
| `PRD.md` | 화면 9종·API 명세·수용 기준 |
| 2_프로젝트기획서_온코케어.docx | 배경·가치제안·사업모델 |
| 3_데이터분석정의서_온코케어.docx | EDA 3건·모델링 3건·타깃 정의 |
| 4_데이터파이프라인정의서_온코케어.docx | S1~S6·결함 6건·스키마 |
| 5_활용공공데이터_목록_온코케어.xlsx | 데이터 22종·ID·결합 키 |
| 3_온코케어_브랜드_가이드.pdf | 색상·톤앤매너·금지 표현 |
