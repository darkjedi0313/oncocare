# PRD — 온코케어(OncoCare)

> 개발 규칙은 `CLAUDE.md`. 이 문서는 무엇을 만들지 정의한다.
> 작성 2026.08.14 · 대상 릴리스 MVP (2026.10)

---

## 1. 제품 정의

### 한 문장

예산이 어디로 가야 하는지 여건을 통제해 판단하고, 그 예산이 효과가 있었는지 검증한다.

### 사용자

| 구분 | 사용자 | 조회 범위 | 주 사용 화면 |
|---|---|---|---|
| 1차 | 국립암센터 · 보건복지부 | 전국 | [0] [1] [2] [3] |
| 2차 | 지역암센터 13 · 시·도 17 | 권역 | [0] [1] [2] [3] |
| 3차 | 보건소 252 | 관할 시군구 | 전 화면 |

**같은 화면에서 조회 범위만 달라진다.** 화면을 두 벌 만들지 않는다.

### 해결하는 문제

```
① 지역끼리 비교가 안 된다      → 여건 유사 20곳 자동 매칭
② 원인을 잘못 짚을 수 있다      → 도농×소득 9개 층 층화
③ 효과를 증명한 사례가 없다     → 무작위 분할로 자연 대조군 생성
```

---

## 2. 데이터 파이프라인 (S1~S6)

### 목표

공공데이터 5종 → `data/processed/segments.parquet` (89,299행)

### 단계별 산출물

| 단계 | 처리 | 출력 | 행수 |
|---|---|---|---|
| S1 | 원천 로드 (인코딩·시트 분기) | `interim/s1_*.pkl` | 24,048 (wide) |
| S2 | 시도명 표준화 · 공백 제거 · 키 생성 | `interim/s2_keys.pkl` | 24,048 |
| S3 | 검진기관·소득·적용인구 결합 | `interim/s3_merged.pkl` | 24,048 |
| S4 | 암종 6개 long 전개 | `interim/s4_long.pkl` | **89,299** |
| S5 | 파생 피처 15종 + 시차변수 | `interim/s5_features.pkl` | 37,672 (시차 유효) |
| S6 | 시간 분할 | `processed/train.parquet` / `test.parquet` | 18,859 / 18,813 |

### 원천 데이터 (`data/raw/`)

| 파일 | ID | 용도 |
|---|---|---|
| 국민건강보험공단_시군구별_성별_암검진_대상_및_수검인원_현황_20241231.xlsx | 15126846 | 타깃 Y |
| 국민건강보험공단_시군구별_검진기관_현황_20241231.xlsx | 빅데이터실 | 접근성 |
| 국민연금공단_자격_시구신고_평균소득월액_20241231.csv | 3046077 | 소득 |
| 국민건강보험공단_월별_시군구별_성별_연령별_직역별_건강보험_적용인구_20241231.csv | 빅데이터실 | 인구·고령·직역 |
| 국민건강보험공단_시군구별_국가건강검진_현황자료(일반검진 암검진)_20231231.zip | 15149732 | 일반검진 연계 |

### 검증 게이트 — 통과 못하면 다음 단계 진행 금지

```
S2   시도 종류 17개 · 후행 공백 0건
S3   검진기관 매칭 ≥ 99% · 소득 ≥ 85% · 적용인구 ≥ 99%
S4   행수 89,299 ± 100 · 수검률 0~100 범위 이탈 0건
S5   피처 15개 결측 0건
S6   학습·검증 타깃 평균 차이 ≤ 1.0%p (분포 이동 없음)
```

`pipeline/run.py`가 각 단계 후 검증 로그를 출력한다.

---

## 3. 분석 산출물 (`analysis/build_artifacts.py`)

API가 읽는 사전 계산 결과. **API에서 모델을 다시 돌리지 않는다.**

| 산출물 | 내용 | 형식 |
|---|---|---|
| `expectation.parquet` | 세그먼트별 여건 기대치 · 잔차 · 회복 여지 | 18,813행 |
| `similar_regions.json` | 시군구별 유사 20곳 목록 + 여건 프로필 | 251 entries |
| `similar_rates.parquet` | 세그먼트별 유사 20곳 실제 가중평균 · 개별 min/max/SD | |
| `strata.parquet` | 암종 × 도농 × 소득3분위 9개 층 교차표 | |
| `coefficients.json` | 선형회귀 계수 + 해석 문구 + 주의사항 | |
| `national_avg.parquet` | 암종 × 성별 × 연령 전국 평균 | |
| `trend.parquet` | 연도별 암종 수검률 (2020·2022·2023·2024) | |
| `linkage_gap.parquet` | 일반검진 대비 암검진 연계 격차 | |
| `actions.json` | 개입 카드 (문헌·우수사례 근거 포함) | |

### 모델 명세

```python
# analysis/expectation.py
FEATURES = ["연령n","여자",
            "암_위암","암_대장암","암_간암","암_유방암","암_자궁경부암","암_폐암",
            "소득로그","인구로그","군","구","기관밀도","관내0","기관전체밀도"]

model = HistGradientBoostingRegressor(
    max_iter=300, learning_rate=0.08, random_state=42)
```

**기대 성능** — R² ≥ 0.85 · MAE ≤ 4.5%p. 미달 시 원인을 확인하고 문서에 기록한다.

```python
# analysis/similar.py
SIM_VARS = ["소득로그","인구로그","군","구","기관전체밀도"]
# StandardScaler → NearestNeighbors(n_neighbors=21) → 자기 제외 20곳
```

**검증** — 20곳 평균의 표준오차 ≤ 2.0%p · 도농 일치율 ≥ 95%

```python
# analysis/strata.py
# 도농(군/시/구) × 소득 3분위 = 9개 층
# 층당 대상자 1,000명 미만인 층은 제외
# 전 층 동일 방향이면 진성 효과, 단순비교와 5%p 이상 차이나면 교란
```

---

## 4. API 명세

Base: `/api` · 응답 `application/json` · 오류 `{"detail": "..."}`

### 공통 쿼리 파라미터

| 이름 | 타입 | 기본 | 설명 |
|---|---|---|---|
| `scope` | enum | `sgg` | `nation` / `sido` / `sgg` |
| `region` | str | — | `scope`에 맞는 키. sgg는 `시도\|시군구` |
| `year` | int | 2024 | |
| `cancer` | str? | — | 미지정 시 전체 |
| `sex` | str? | — | |
| `age` | str? | — | |

---

### 조회 엔드포인트 — LLM 호출 금지

#### `GET /api/summary` — 화면 [0]

```json
{
  "region": "서울특별시|양천구",
  "year": 2024,
  "denominator_note": "건강보험 가입자 기준",
  "total": { "rate": 55.5, "target": 378581, "screened": 210141,
             "unscreened": 168440, "national": 56.3, "rank": 148, "of": 251 },
  "by_cancer": [
    { "cancer": "간암", "rate": 71.9, "national": 76.1, "flag": "low" }
  ],
  "top_segments": [
    { "sex": "여자", "age": "65~69", "cancer": "대장암",
      "rate": 41.9, "similar": 51.0, "gap": -9.1, "recovery": 1034 }
  ]
}
```

`flag` — 전국 평균보다 5%p 이상 낮으면 `low`. 화면에서 Alert Coral로 표시.

#### `GET /api/priority` — 화면 [1]

```json
{
  "filters": { "min_recovery": 300, "max_age": "70~74" },
  "items": [
    { "rank": 1, "region": "대구광역시|수성구",
      "sex": "여자", "age": "50~54", "cancer": "대장암",
      "rate": 20.2, "expected": 33.1, "similar": 31.5,
      "national": 37.1, "prev_year": 22.8,
      "gap": -12.9, "recovery": 1604,
      "similar_range": [24.1, 38.7], "similar_se": 0.9,
      "percentile_in_similar": 8,
      "elasticity": "낮음" }
  ]
}
```

**정렬 — `recovery` 내림차순.** 격차가 아니라 회복 여지로 정렬한다.

#### `GET /api/factors` — 화면 [2]

```json
{
  "segment": { "region": "...", "sex": "...", "age": "...", "cancer": "..." },
  "changeable": [
    { "factor": "관내 대장내시경 가능 기관 부족", "effect": -3.8,
      "source": "층화 9개 층 가중평균", "detail_url": "/api/factors/strata?..." }
  ],
  "fixed": [
    { "factor": "연령 구조", "effect": -2.8 },
    { "factor": "소득 수준", "effect": 1.2,
      "note": "국가검진 밖 종합검진 수검이 통계에 포함되지 않을 가능성" }
  ],
  "strata_table": [
    { "stratum": "구·소득하", "absent": 48.0, "present": 56.5, "diff": 8.5,
      "absent_target": 12045 }
  ],
  "cautions": [
    "관내 기관 부재 변수는 기관밀도와 종속 관계로 트리 모델 중요도는 0입니다. 계수로 확인된 효과입니다.",
    "소득 계수는 로그 스케일이므로 %p로 직접 해석하지 않습니다."
  ]
}
```

**`strata_table`을 화면에 그대로 공개한다.** 담당자가 검증할 수 있어야 한다.

#### `GET /api/compare` — 화면 [3]

```json
{
  "region": "서울특별시|양천구",
  "profile": { "income": 1544422, "population": 189000,
               "urban": "구", "inst_density": 11.1 },
  "similar_regions": [
    { "region": "서울특별시|노원구", "income": 1520000, "population": 201000,
      "rate": 48.2 }
  ],
  "stats": { "mean": 51.0, "min": 44.0, "max": 57.9, "se": 0.82,
             "our_rate": 41.9, "our_percentile": 15 },
  "note": "여건 유사 20곳은 소득·인구·도농·전체 검진기관 밀도가 가까운 지역입니다."
}
```

**20곳 전체 목록을 반환한다.** 화면에서 접을 수 있게 하되 데이터는 전부 보낸다.

#### `GET /api/actions` — 화면 [4]

```json
{
  "segment": { "...": "..." },
  "rate": 20.2,
  "rule_applied": "rate < 30",
  "actions": [
    { "title": "우편 FIT 발송",
      "why": "기저 수검률 30% 미만 구간에서 가장 효과적",
      "evidence": "네트워크 메타분석 RCT 76건 (Gastroenterology 2026) RR 3.12",
      "expected": "실제 운영 조건 +3~7%p",
      "caution": "실행률이 낮으면 효과가 −7.4%p까지 나타난 사례가 있습니다" },
    { "title": "약국·경로당·보건지소 채변통 사전 배부",
      "why": "대변 채취 부담이 최대 장벽인 세그먼트",
      "evidence": "진천군보건소 우수사례" }
  ],
  "survey_questions": [
    "결과 안내문 받으셨나요?", "안 받으신 이유가 있나요?", "검사 비용이 걱정되나요?"
  ]
}
```

**개입 규칙**

```
IF 수검률 < 30%   → 우편 FIT 발송 우선 (RR 3.12)
ELSE              → 안내 개선 + 문자 발송 (+6.6%p)
공통              → 브랜드 포장에 자원 쓰지 않음 (효과 없음)
공통              → 실행률 모니터링 필수
```

#### `GET /api/records` — 화면 [8] 조회

```json
{
  "region": "...", "year": 2026,
  "campaigns": [
    { "id": 12, "segment": {...}, "created": "2026-03-02",
      "contact_n": 300, "control_n": 6197,
      "contacted": 287, "reached": 201,
      "reasons": { "안내문_미수신": 34, "이유있음": 92, "비용우려": 51 },
      "sms_sent": 6497, "sms_date": "2026-03-10" }
  ]
}
```

---

### 생성 엔드포인트 — LLM 사용

#### `POST /api/message` — 화면 [6]

Request
```json
{ "region": "서울특별시|양천구", "sex": "여자", "age": "65~69",
  "cancer": "대장암", "tone": "standard" }
```

Response
```json
{
  "text": "[양천구보건소] ○○○님, 올해 대장암 검진을 아직 받지 않으셨습니다. ...",
  "facts_used": [
    { "label": "검진 주기", "value": "1년" },
    { "label": "본인부담", "value": "건강보험료 하위 50% 무료" },
    { "label": "채변통 수령처", "value": "보건소·보건지소·인근 약국" }
  ],
  "reflected": [
    "1년 주기 명시 — 다른 암은 2년이라 혼동이 많음",
    "채변통 사전 수령 안내 — 진천군 사례",
    "비용 안내 — 무료 대상 미인지 사례가 많음"
  ],
  "warning": "발신 기관명은 보건소입니다. 온코케어를 노출하지 않습니다."
}
```

**생성 규칙**
- 수치는 조회 결과를 프롬프트에 명시하고 그 값만 사용하게 한다
- 생성 후 프롬프트에 없는 숫자가 출력에 있으면 재생성 (최대 2회) 후 실패 시 오류 반환
- 「암일 수 있다」류 표현 금지
- 검진 대상이 아닌 조건이면 400 반환

#### `POST /api/report` — 화면 [7]

Request
```json
{ "region": "...", "year": 2026, "format": "docx" }
```

Response — Word 파일 스트림 또는 `{ "text": "...", "sections": [...] }`

**포함 항목** — 성과지표 · 수행내용 · 미달사유 · 개선계획

---

### 표본 추출 — 화면 [5]

#### `POST /api/sample`

Request
```json
{ "region": "...", "sex": "여자", "age": "65~69", "cancer": "대장암",
  "n": 300, "seed": 42 }
```

Response
```json
{ "campaign_id": 12,
  "segment": {...},
  "total_unscreened": 6497,
  "contact_n": 300, "control_n": 6197,
  "assignment_method": "무작위 (seed 고정)",
  "note": "전원 연락이 불가능하므로 잔여 대상이 자연 대조군이 됩니다. 추가 비용이 발생하지 않습니다.",
  "download": "/api/sample/12/list"
}
```

**개인 명단을 생성하지 않는다.** 세그먼트와 배정 인원만 기록하고, 실제 대상자 명단은 보건소가 자체 시스템에서 동일 조건·동일 시드로 추출한다.

#### `POST /api/records` — 화면 [8] 기록

Request
```json
{ "campaign_id": 12, "contacted": 287, "reached": 201,
  "reasons": { "안내문_미수신": 34, "이유있음": 92, "비용우려": 51 },
  "sms_sent": 6497, "sms_date": "2026-03-10" }
```

---

## 5. 화면 명세

### 공통 레이아웃

```
데스크톱 1920 × 1080 · 세로 스크롤 없이 한 화면
좌측 사이드바 260px  (분석 5 / 구분선 / 실행 4)
메인 영역 1660px      좌 62% / 우 38% 2단 그리드
상단바 64px           지역 선택 · 기준연도 · 담당자
우측 하단             「온코」 플로팅 72px (디자인만, 클릭 시 2차 개발 안내)
하단 여백 96px        플로팅 버튼과 콘텐츠 겹침 방지
```

**하단 출처 표기** — `데이터 출처 — 국민건강보험공단 … / 국민연금공단 … / 질병관리청 …`
**「©」 기호를 사용하지 않는다.**

### 화면 목록

| # | 메뉴명 | API | LLM | 우선순위 |
|---|---|---|---|---|
| 0 | 우리 구 현황 | `GET /api/summary` | — | **상** |
| 1 | 어디부터 할까 | `GET /api/priority` | — | **상** |
| 2 | 왜 낮을까 | `GET /api/factors` | — | **상** |
| 3 | 다른 지역은 | `GET /api/compare` | — | 중 |
| 4 | 무엇을 할까 | `GET /api/actions` | — | 중 |
| 5 | 연락 명단 만들기 | `POST /api/sample` | — | 중 |
| 6 | **안내문 만들기** | `POST /api/message` | **사용** | **상** |
| 7 | **보고서 만들기** | `POST /api/report` | **사용** | **상** |
| 8 | **실행 기록** | `GET/POST /api/records` | — | **상** |

### [0] 우리 구 현황

```
요약 카드 3개 (가로)
  6대암 통합 수검률 55.5%  │ 전국 평균 56.3%  │ −0.8%p
  미수검 인원 168,440명    │ 대상 378,581명 중
  전국 순위 148위          │ 251개 시군구 중

좌 62%  암종별 수검률 가로 막대 6개
        뒤 회색 = 전국 평균 / 앞 = 우리 구
        전국 대비 5%p 이상 낮은 암종만 Alert Coral

우 38%  지금 연락해야 할 우선 대상 3건
        세그먼트 · 미수검 · 여건 대비 격차 · 회복 여지
        하단 링크 → [1] 어디부터 할까
```

### [1] 어디부터 할까

```
필터  암종 / 성별 / 연령 · 토글 「미수검 500명 이상만」(기본 켜짐)
      우측 회색 「여건 통제 변수 : 소득 · 인구 · 도농 · 검진기관 밀도」

표    순위 │ 시군구 │ 세그먼트 │ 미수검 │ 우리 구 │ 유사 20곳 │ 전국 │ 차이 │ 회복 여지
      1행만 배경 강조 · 차이 열 Alert Coral · 유사 20곳 열에 [목록] 링크

주의  「회복 여지는 개입 효과 예측이 아니라 여건 수준 도달 시 상한입니다.
      개입 기록이 없어 인과효과를 산출할 수 없습니다.」

하단  [왜 낮을까] [연락 명단 만들기] [무엇을 할까]
```

### [2] 왜 낮을까

```
부제  세그먼트 · 우리 구 20.2% · 유사 20곳 31.5% · 차이 −11.3%p

좌 62%  요인별 기여도 가로 막대
        「바꿀 수 있는 것」 Care Teal / 「바꿀 수 없는 것」 회색
        0선 기준 좌우

우 38%  이렇게 읽으면 됩니다 — 해석 블록 3개
        하단 「권장 다음 단계」 + 우수사례 출처

접이식  [층별 상세 보기] → 9개 층 교차표 전체 공개
```

### [3] 다른 지역은

```
우리 구 여건 프로필 (소득 · 인구 · 도농 · 기관밀도)

유사 20곳 목록 표 — 시군구 │ 소득 │ 인구 │ 수검률
통계 — 평균 51.0% · 범위 44.0~57.9 · 평균 오차 ±1.6%p
      우리 구 41.9% · 20곳 중 하위 15%

주의  「여건 유사 20곳은 소득·인구·도농·전체 검진기관 밀도가 가까운 지역입니다.
      암종별 검진기관 유무는 매칭 변수에 포함되지 않습니다.」
```

### [4] 무엇을 할까

```
좌 62%  개입 카드 3개 — 제목 / 근거 / 예상 효과 / 주의
        각 카드에 문헌 또는 우수사례 출처 표기

우 38%  통화 시 사유 3문항
        「응답이 쌓이면 내년 분석의 정밀도가 올라갑니다」

하단  [안내문 만들기]
```

### [5] 연락 명단 만들기

```
세그먼트 확인 · 미수검 6,497명
연락 가능 인원 입력 (기본 300)

[무작위로 배정하기]
  → 연락군 300명 / 대기군 6,197명
  → 시드 고정값 표시

설명  「어차피 전원에게 연락할 수 없으므로 나머지가 자연 대조군이 됩니다.
      연말에 두 그룹을 비교하면 개입 효과를 측정할 수 있습니다. 추가 비용은 없습니다.」

주의  「개인 명단은 생성하지 않습니다. 동일 조건·동일 시드로 보건소 시스템에서 추출하십시오.」
```

### [6] 안내문 만들기 ★ LLM

```
좌 45%  문자 미리보기 (휴대폰 형태 카드)
우 55%  반영한 내용 체크 목록
        하단 경고 「발신 기관명은 보건소입니다. 온코케어를 노출하지 않습니다.」

하단  [수정] [복사] [다른 톤으로]
```

### [7] 보고서 만들기 ★ LLM

```
좌  보고서 초안 미리보기 (성과지표 / 수행내용 / 미달사유 / 개선계획)
우  사용된 데이터 출처 목록
하단  [Word 내보내기] [수정]
```

### [8] 실행 기록

```
캠페인 목록 표 — 생성일 │ 세그먼트 │ 연락군 │ 대기군 │ 연락 완료 │ 통화 성공 │ 문자 발송

행 클릭 → 입력 폼
  연락 여부 · 일자 · 통화 성공 여부
  사유 3문항 응답 수
  문자 발송 여부 · 일자

안내  「개인 식별정보를 저장하지 않습니다. 세그먼트 단위 집계로만 기록됩니다.」
      「연말 실적이 공개되면 연락군과 대기군의 수검률을 비교할 수 있습니다.」
```

---

## 6. 수용 기준

### 파이프라인

- [ ] `python pipeline/run.py --stage all` 무오류 실행
- [ ] `segments.parquet` 89,299행 ± 100
- [ ] 검진기관 매칭 ≥ 99% · 소득 ≥ 85% · 적용인구 ≥ 99%
- [ ] 수검률 0~100 범위 이탈 0건
- [ ] 원본 파일 교체 후 재실행으로 동일 결과 재현

### 분석

- [ ] 기대치 모델 R² ≥ 0.85 · MAE ≤ 4.5%p
- [ ] 유사 20곳 평균 표준오차 ≤ 2.0%p · 도농 일치율 ≥ 95%
- [ ] 층화 9개 층 교차표 산출 (층당 대상자 1,000명 이상만)
- [ ] 선형회귀 계수 + 주의사항 JSON 생성
- [ ] 모든 수치가 데이터에서 산출 (하드코딩 0건)

### API

- [ ] 조회 6종 200 응답 · 응답 시간 p95 < 1초
- [ ] `tests/test_no_llm_in_query.py` 통과 (조회 라우터에 LLM 참조 0건)
- [ ] 동일 요청 10회 반복 시 응답 동일 (수치 결정성)
- [ ] 생성 2종 성공률 100% · 프롬프트 외 숫자 출력 0건
- [ ] 모든 응답에 `denominator_note` 포함

### 프론트엔드

- [ ] 화면 9종 렌더링 · 좌측 메뉴 9개 (5+4 그룹)
- [ ] 1920×1080에서 세로 스크롤 없이 표시
- [ ] Alert Coral 사용 화면당 3개 이하
- [ ] 모든 수검률 표시에 「건강보험 가입자 기준」 병기
- [ ] 「이 값은 평가가 아니라 검토 시작점입니다」 문구 고정 표시
- [ ] 하단 출처 표기에 「©」 미사용
- [ ] 「온코」 버튼 클릭 시 2차 개발 안내 표시

### 기록

- [ ] 개인 식별정보 컬럼 0개 (스키마 검증)
- [ ] 캠페인 생성 → 기록 입력 → 조회 왕복 동작

---

## 7. 개발 일정

| 기간 | 단계 | 산출물 |
|---|---|---|
| W1 | 파이프라인 S1~S6 | `segments.parquet` + 검증 로그 |
| W2 | 분석 산출물 | 9종 parquet/json |
| W3 | API 조회 6종 + 화면 [0][1][2] | 조회만으로 동작하는 웹 |
| W4 | API 생성 2종 + 화면 [3]~[7] | LLM 기능 포함 |
| W5 | [8] 실행 기록 + 배포 + 검증 | Railway 배포 URL |

---

## 8. 범위 밖 (Out of Scope)

```
❌ 「온코」 대화 기능 — 화면 디자인까지만. 2차 개발
❌ 개인 위험도 예측 — 의료기기 규제 대상
❌ 개인 명단 생성·저장
❌ uplift 모델 — 개입 기록 부재. 파일럿 후
❌ 실시간 데이터 연동 — 공공데이터 갱신 주기 연 단위
❌ 시군구 암발생률 결합 — 미개방
❌ 모바일 반응형 — 데스크톱 전용
❌ 다국어
```

---

## 9. 알려진 한계 — 화면과 문서에 명시

| 한계 | 표기 위치 |
|---|---|
| 개입 효과를 산출할 수 없다 (개입 기록 부재) | [1] 주의 박스 |
| 검진기관 데이터 기준일 2020.12월말 (4년 시차) | [2] 각주 |
| 분모가 보건소 성과지표와 다르다 | 전 화면 하단 |
| 고소득 지역은 국가검진 외 수검이 미포함 | [2] 해석 블록 |
| 암종별 기관 유무가 유사 매칭 변수에 없다 | [3] 주의 |
| 지역 여건의 설명력은 전체의 약 5% | 문서 |
