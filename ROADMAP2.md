# ROADMAP.md — 5일 개발 계획 (개편안)

> 기능 명세 `PRD.md` · 실행 방법 `DEVELOPMENT.md` · 에이전트 규칙 `AGENTS.md`
> 작성 2026.08.18 (개정) · 개발 기간 5일 · 발표 2026.08.26

---

## 📌 전제 — 5일 개발 계획으로의 개편 (초고속 개발 반영)

첫째 날에 기존 3일치 계획(데이터 파이프라인 S1~S6, 분석 산출물, 조회 API 4종, 화면 [0][1][2] 동적 구현 및 연동)을 모두 완료하였습니다. 이에 따라, 기존에 버리거나 스켈레톤(Mock-up) 처리하기로 했던 기능들을 **실제 작동하는 완전한 기능으로 격상하여 전면 구현**하는 5일 개발 일정을 수립합니다.

```
[Day 1] (완료) 파이프라인 · 분석 산출물 · 조회 API · 화면 [0][1][2]
[Day 2] (완료) 다른 지역 비교 [3] · 개입 카드 추천 [4] · 무작위 대조군 샘플링 [5]
[Day 3] (완료) LLM 기반 문자 문안 생성 [6] · 보고서 초안 생성 [7]
[Day 4] (완료) SQLite 연동 실행 기록 저장 및 조회 [8] · UI 수동 입력 폼
[Day 5] (완료) 전체 통합 검증 (pytest) · 시연 시나리오 리허설 · README 정리
[Day 5+] (완료) 상급기관 전국 모니터링 [9] 신설 · Word/PDF 이중 다운로드 · 개입 전략 다변화 및 UI 최적화
```

---

## 🏆 최소 시연 가능 상태 (MDS)
**이것이 달성되면 발표가 성립하며, 현재 120% 초과 완수되었습니다.**

1. `segments.parquet` 89,299행 완벽 생성 및 검증 통과
2. HistGradientBoosting Regressor 모델 $R^2$ 0.8809 재현 완료
3. 화면 [0] 우리 구 현황 — 실제 API 데이터 및 듀얼 막대 차트 연동 완료
4. 화면 [1] 어디부터 할까 — 우선순위 세그먼트 표 렌더링 및 유사 지역 매칭 모달 완료
5. 화면 [2] 왜 낮을까 — 층화 통제 요인 분석 기여도 양방향 차트 및 아코디언 표 연동 완료
6. 화면 [9] 전국 모니터링 — 상급기관용 251개 지자체 격차 정렬 및 시도 필터링, 상세 모달 연동 완료
7. 화면 [7] 보고서 만들기 — 관공서 기안문 서식 디자인 개편, 직인 CSS 연동 및 PDF/Word 내보내기 완료


---

## 📅 일차별 상세 일정 및 완료 기준

### 📅 Day 1 — 파이프라인 + 분석 산출물 + 조회 API + 화면 [0][1][2] (완료)
* **산출물**: 
  * `segments.parquet` (89,299행) 및 `strata.parquet` (층화 분석 교차표) 생성 완료.
  * API 4종 (`/api/summary`, `/api/priority`, `/api/compare`, `/api/factors`) 개발 완료.
  * 화면 [0], [1], [2] 동적 바인딩 및 양방향 차트 렌더링, 층화 표 아코디언 연동 완료.
  * `pytest` 16개 항목 전체 통과 완료.

---

### 📅 Day 2 — 개입 전략 및 대상 추출 (실제 연동)
* **목표**: 백엔드가 이미 구현된 다른 지역 비교 화면을 완성하고, 규칙 기반의 개입 및 무작위 추출 화면을 실제 API와 연동합니다.

| 블록 | 작업 내용 | 완료 기준 (Acceptance Criteria) |
|---|---|---|
| 2-1 | 화면 [3] 다른 지역은 고도화 | `/api/compare` 연동 및 여건 유사 20곳 상세 스펙 테이블 렌더링 |
| 2-2 | `GET /api/actions` + 화면 [4] | 수검률 구간별 개입 카드 추천 로직 및 문헌/우수사례 근거(Evidence) 노출 |
| 2-3 | `POST /api/sample` + 화면 [5] | 무작위 난수 시드(Seed)를 고정한 타깃군(300명 등) vs 대기군(자연 대조군) 분할 로직 구현 |

> **⚠️ 개인정보 절대 보호**: 화면 [5]에서 절대 실제 개인 명단을 생성하거나 저장하지 않습니다. 세그먼트 키와 배정된 인원 수, 난수 시드값만 메모리 및 DB에 관리하고 명단 추출은 보건소 자체 시스템에 위임합니다.

---

### 📅 Day 3 — LLM 기능 구현
* **목표**: Gemini API와 LangChain을 결합하여 생성형 AI 비즈니스 로직 및 화면 연동을 완료합니다.

| 블록 | 작업 내용 | 완료 기준 (Acceptance Criteria) |
|---|---|---|
| 3-1 | `api/llm.py` Gemini 래퍼 설계 | 프롬프트 외부 숫자 생성 차단 필터 및 최대 2회 재생성(Retry) 로직 구현 |
| 3-2 | `POST /api/message` + 화면 [6] | 세그먼트별 맞춤형 문자 초안 생성, 화면 내 보건소 발신 및 온코케어 비노출 경고 명시 |
| 3-3 | `POST /api/report` + 화면 [7] | 분석 데이터를 요약 인용하는 연말 보고서 초안 생성 및 렌더링 |

> **⚠️ 주의 사항**: 생성형 AI가 숫자를 마음대로 창작하지 못하도록 하는 수치 검증 장치를 3-1에서 반드시 선구현해야 합니다.

---

### 📅 Day 4 — 실행 기록 및 SQLite 연동
* **목표**: 개입 활동의 집계를 기록하고 효과 검증의 기반이 되는 데이터베이스(SQLite) 레이어 및 화면 연동을 완료합니다.

| blocks | 작업 내용 | 완료 기준 (Acceptance Criteria) |
|---|---|---|
| 4-1 | SQLite DB 스키마 설계 및 연동 | `api/database.py` 구성, 캠페인 및 실행 기록 테이블 설계 (개인정보 컬럼 0개 확인) |
| 4-2 | `GET/POST /api/records` 구현 | 개입 캠페인 생성, 연락 인원, 통화 성공 및 미수검 사유 3문항 집계 응답 엔드포인트 구현 |
| 4-3 | 화면 [8] 실행 기록 연동 | 캠페인 리스트 조회, 행 클릭 시 수동 실적 및 사유 집계 입력 폼 동적 연동 |

---

### 📅 Day 5 — 통합 검증 및 시연 준비 (완료)
* **목표**: 최종 프로덕션 안정성을 확보하고 시연 시나리오를 고도화합니다.

| blocks | 작업 내용 | 완료 기준 (Acceptance Criteria) |
|---|---|---|
| 5-1 | 전체 회귀 테스트 수행 | `pytest`를 수행하여 조회 API의 LLM 비사용 검증, 10회 반복 결정성 검증 등 20개 이상 테스트 통과 |
| 5-2 | 표현 규칙 일괄 점검 | 모든 화면에 분모 기재("건강보험 가입자 기준"), 경고 문구("이 값은 평가가 아니라..."), Alert Coral 3개 이하 제한 확인 |
| 5-3 | 리허설 및 캡처 백업 | 5분 시연 시나리오를 바탕으로 로컬 기동 상태 검증 및 발표 자료용 캡처 이미지 백업 생성 |
| 5-4 | README 및 커밋 정리 | 가이드라인 정리 및 최종 코드 정리 |

---

### 📅 Day 5+ — 상급기관 관리 및 내보내기 기능 고도화 (추가 완료)
* **목표**: 상급 기관용 전국 모니터링 화면을 추가하고, 공공 서식 보고서의 완성도를 PDF 파일 출력과 직인 인장 연동으로 완성합니다.

| blocks | 작업 내용 | 완료 기준 (Acceptance Criteria) |
|---|---|---|
| 5+-1 | 화면 [9] 전국 모니터링 신설 | `/api/priority` 데이터 기반 251개 지자체 수검 격차 롤업 집계, 시도 필터링 및 세그먼트 상세 진단 모달 팝업 구현 |
| 5+-2 | 보고서 PDF 내보내기 지원 | `html2pdf.js` 탑재, 직인 도장 및 결재란이 포함된 공문서 서식 그대로 PDF 파일 즉시 저장 연동 |
| 5+-3 | 개입 전략 암종별 세분화 | 6대암(위, 대장, 간, 유방, 자궁경부, 폐) 특성에 맞는 행동 전략 분기 매핑 및 무엇을 할까 화면 표 연동 |

---

## 🚨 최종 점검 체크리스트
발표 직전 반드시 아래의 모든 항목에 체크표시(`[x]`)가 되어 있어야 합니다.

* [x] 파이프라인 재실행 시 동일한 `segments.parquet` (89,299행) 복제 가능성
* [x] 기대치 회귀 모델 검증 데이터 기준 $R^2 \ge 0.85$, $MAE \le 4.5\%p$ 재현 확인
* [x] `api/routers/query.py` 파일 내에 `llm` 관련 모듈 import 차단 완료
* [x] 모든 화면에 "건강보험 가입자 기준" 분모 정보가 표기되었는지 확인
* [x] 모든 화면 하단 또는 사이드바에 "이 값은 평가가 아니라 검토 시작점입니다." 경고 고정 배치
* [x] 모든 화면 하단 출처 표기에 `©` 기호 배제 완료
* [x] SQLite 및 기록 데이터베이스 구조에 개인 식별 정보(이름, 연락처 등) 컬럼이 0개인지 검증
* [x] 안내문 만들기 [6] 화면 하단에 보건소 발신 경고 표시가 나타나는지 검증

---

## 🎬 시연 시나리오 — 5분
발표 당일 아래의 동선으로 라이브 시연을 진행합니다.

1. **[0] 우리 구 현황**: 양천구의 6대암 통합 수검률과 전국 순위, 가장 부진한 암종(대장암, Alert Coral 강조)을 요약 브리핑합니다.
2. **[1] 어디부터 할까**: 유사 20곳 대비 격차가 가장 크고 회복 여지가 1,000명이 넘는 1순위 타깃 세그먼트(여성 65~69세 대장암)를 발굴합니다. [목록] 링크를 눌러 유사 시군구 20곳 리스트를 띄웁니다.
3. **[2] 왜 낮을까**: 해당 세그먼트의 부진 원인이 "관내 대장내시경 검진기관 부족"에 있음을 양방향 기여도 차트로 브리핑하고, 하단 아코디언을 열어 도농/소득 9개 층에서도 일관되게 나타남을 보여줍니다.
4. **[3] 다른 지역은**: 여건 유사 20곳의 소득/인구/기관밀도 등 상세 지표 대비 우리 구의 순위 프로필을 확인합니다.
5. **[4] 무엇을 할까**: 기저 수검률에 맞춤화된 "우편 FIT 발송" 전략 카드와 근거 논문 출처를 검토하고, 통화 시 기록할 설문 3문항을 확인합니다.
6. **[5] 연락 명단 만들기**: 난수 시드를 활용해 연락 대상군(300명)과 자연 대조군을 무작위 분할하는 원리를 설명합니다.
7. **[6] 안내문 만들기**: Gemini AI를 통해 혼동 방지 주기(1년)와 무료 대상을 강조한 맞춤형 안내 메시지를 자동 생성합니다.
8. **[7] 보고서 만들기**: 요약된 수치들을 기반으로 연말에 보고서 작성의 수고를 덜어줄 성과 보고서 초안을 자동으로 빌드합니다.
9. **[8] 실행 기록**: 실제 개입 후 연락 결과 및 사유 설문 집계를 데이터베이스에 보관하고, 대조군 대비 효과를 연말 실적 공개 시 추적할 수 있음을 보여줍니다.

---

## 🗂️ 일일 종료 루틴
매일 개발 작업 종료 15분 전에 반드시 수행합니다.
1. `pytest` 실행 결과 기록
2. Git Commit 분할 수행 및 푸시
3. 내일 첫 작업을 한 줄로 [PROGRESS.md](file:///c:/Users/USER/project_ys/oncare/PROGRESS.md)에 기록해두기
