# 온코케어가 쓰는 데이터 분석 모델, 아주 자세히 풀어드릴게요

이 문서는 앞서 드린 요약 설명을 훨씬 더 깊게, 실제 데이터 숫자를 그대로 넣어서 다시 쓴 버전입니다. "왜 그렇게 계산되는지"를 손으로 따라갈 수 있도록 실제 산출물 파일(`data/processed/`)에서 뽑은 값을 그대로 인용했습니다.

---

## 0. 먼저, 이 서비스의 데이터가 어떻게 생겼는지부터

온코케어의 모든 계산은 **"세그먼트"**라는 작은 조각 위에서 이뤄집니다. 세그먼트 하나는 다음 4가지 기준으로 데이터를 쪼갠 결과입니다.

```
시군구(예: 제주시) × 성별(여자) × 연령대(55~59세) × 암종(대장암) = 세그먼트 1개
```

이렇게 쪼개면 전국에서 **약 3만 7천~8만 9천 개**(연도에 따라 다름)의 세그먼트가 생기고, 각 세그먼트마다 실제로 기록된 값은 딱 3가지뿐입니다.

| 컬럼 | 의미 |
|---|---|
| 대상자 | 이 조건에 해당하는 건강보험 가입자 수 (검진 받을 자격이 있는 사람 수) |
| 수검자 | 그중 실제로 검진을 받은 사람 수 |
| 수검률 | 수검자 ÷ 대상자 × 100 (%) — **이 값을 맞히는 것이 모든 모델의 목표** |

여기에 그 지역의 "여건" 정보(소득, 인구, 검진기관 밀집도 등)를 15가지 결합해서 붙입니다. 이 15가지 여건 + 정답(수검률)을 갖고 4개의 서로 다른 분석을 돌리는 것이 온코케어 모델의 전부입니다.

> **왜 "여건"만 쓰고 개인 정보는 안 쓰나요?** — CLAUDE.md 규칙상 이름·주민번호·개인 수검 이력 등은 저장하지 않습니다. 세그먼트 단위(집단) 통계만 다루기 때문에 개인을 추적하거나 식별할 수 없습니다.

---

## 1. 실제 세그먼트 하나를 처음부터 끝까지 따라가보기

말로만 설명하면 감이 안 오니, 실제 2024년 데이터 한 조각을 골라서 모델 ①이 어떻게 판단하는지 끝까지 따라가 보겠습니다.

**고른 세그먼트**: 제주특별자치도 · 제주시 · 여자 · 55~59세 · 대장암

| 항목 | 값 |
|---|---|
| 대상자 | 12,570명 |
| 수검자 | 4,507명 |
| **실제 수검률** | **35.86%** (4,507 ÷ 12,570 × 100) |
| 소득로그 | 14.16 (소득 수준을 로그 변환한 값 — 아래에서 설명) |
| 인구로그 | 12.96 |
| 기관밀도 | 0.52 |
| 관내0 (관내 기관 부재) | 0 (있음) |
| 기관전체밀도 | 1.13 |

이 6개 여건 정보(+나머지 9개, 표에는 생략)를 모델 ①에 넣으면:

```
모델 ① 예측 결과 (기대치) = 41.87%
```

즉 "제주시의 소득·인구·기관밀도 등 여건을 보면, 이 나이대 여성의 대장암 수검률은 원래 41.87% 정도가 나와야 정상"이라는 뜻입니다. 그런데 실제로는 35.86%밖에 안 나왔으니:

```
잔차 = 실제(35.86) − 기대치(41.87) = −6.02%p
회복여지 = 6.02%p × 대상자(12,570) ÷ 100 ≈ 756명
```

이 **756명**이라는 숫자가 화면에 "회복 여지 756명"으로 나오는 겁니다. "여건이 비슷한 다른 곳들만큼만 받아도 756명이 더 검진받을 수 있다"는 뜻이지, "정답은 41.87%다"라는 강제 목표가 아닙니다.

---

## 2. 모델 ① 기대치 모델 — 자세히 뜯어보기

### 2-1. 원리: "나무를 여러 개 겹쳐 쌓기" (그레이디언트 부스팅)

`HistGradientBoostingRegressor`라는 이름이 복잡해 보이지만, 원리는 단순합니다.

**1단계** — 아주 단순한 예측 나무 하나를 만듭니다. 예: "연령n이 8보다 크면 55%, 아니면 60%로 예측"처럼 대충 큰 갈래로만 나눕니다. 당연히 많이 틀립니다.

**2단계** — 1단계가 틀린 만큼(오차)만 따로 떼서, 그 오차를 맞히는 두 번째 나무를 또 만듭니다.

**3단계** — 또 남은 오차를 세 번째 나무가 맞힙니다.

이걸 **300번** 반복합니다(`max_iter=300`이 이 뜻입니다). 나무 하나하나는 아주 단순하지만, 300개를 순서대로 겹쳐 쌓으면 "소득도 낮고 인구도 적고 검진기관도 없으면 유독 더 떨어진다"처럼 여러 조건이 동시에 맞아떨어질 때의 복잡한 패턴까지 잡아낼 수 있습니다. 이걸 "여러 명의 전문가가 순서대로 서로의 실수를 보완해가며 예측하는 것"에 비유할 수 있습니다.

`learning_rate=0.08`은 "한 나무가 오차를 얼마나 과감하게 고쳐볼지"를 정하는 값입니다. 너무 크면 성급하게 고치다 오히려 틀리고(과적합), 너무 작으면 300번을 반복해도 충분히 못 배웁니다. 0.08은 실험을 통해 정한 값입니다.

### 2-2. 성능을 어떻게 "숫자로" 확인하나 — R²와 MAE

**R² (결정계수)** — "이 모델이 수검률 차이를 얼마나 잘 설명하는가"를 0~1 사이 숫자로 나타낸 것. 쉽게 말해 100점 만점 시험 점수라고 생각하면 됩니다.
- R²=0 → 그냥 평균값으로 찍는 것과 다를 바 없음
- R²=1 → 완벽하게 맞힘 (현실에서는 거의 불가능하고, 오히려 의심해야 함 — 아래 "왜 R²가 너무 높아도 안 되는가" 참고)
- 온코케어 모델 ①의 실측 R² = **0.8809** → "지역마다 수검률이 왜 다른지"의 88% 정도를 이 15개 여건으로 설명할 수 있다는 뜻

**MAE (평균 절대 오차)** — 예측이 평균적으로 몇 %p씩 틀리는지. 실측 MAE = **4.29%p** → "평균적으로 실제 수검률과 4.29%p 정도 차이가 난다"는 뜻입니다. 100점 시험에서 "평균 오차 4.29점"이라고 생각하면 감이 옵니다.

### 2-3. 시험을 "부정행위 없이" 봤는지 확인하기 (학습/검증 분리)

이 모델을 만들 때 가장 중요한 규칙은 **"공부한 문제로 시험 보지 않기"**입니다.

```
2023년 데이터(18,851개 세그먼트) → 공부(학습)
2024년 데이터(18,896개 세그먼트) → 한 번도 안 보여준 시험(검증)
```

만약 학습과 검증에 같은 연도 데이터를 무작위로 섞어서 나눴다면(`train_test_split`), 같은 지역·같은 암종의 데이터가 학습에도 검증에도 살짝씩 섞여 들어가서 점수가 실제보다 좋게 나오는 "부정행위" 효과가 생깁니다. CLAUDE.md 규칙 5가 이걸 금지하는 이유입니다.

**추가로 직접 확인한 것 — 과적합 점검**

| 구분 | R² |
|---|---|
| 공부한 데이터(2023) 자체 점수 | 0.9203 |
| 시험(2024) 점수 | 0.8809 |
| 차이 | 0.0393 |

공부한 문제 점수(0.92)가 시험 점수(0.88)보다 높은 건 자연스러운 현상입니다(어느 모델이든 공부한 데이터에서 더 잘함). 차이가 0.04 정도로 크지 않다는 건 "달달 외운 게 아니라 실제로 패턴을 이해했다"는 뜻입니다. 만약 이 차이가 0.2, 0.3처럼 컸다면 "과적합"(외운 것만 잘하고 새 문제는 못 푸는 상태)을 의심해야 합니다.

한 번 더 확인하기 위해, 2023년 데이터만 갖고 5등분해서 서로 돌아가며 시험 보는 방식(5-Fold 교차검증)도 해봤습니다. 결과는 0.887~0.903 사이, 평균 0.894로 안정적이었습니다. 이 역시 "우연히 우리가 고른 시험 문제(2024년)만 어쩌다 잘 맞은 게 아니다"라는 걸 뒷받침합니다.

### 2-4. 이 모델이 특히 잘 틀리는 구간 — 실제로 찾아낸 약점

전체 성적은 기준(R²≥0.85, MAE≤4.5%p)을 통과하지만, 모든 세그먼트를 똑같이 잘 맞히는 건 아닙니다. 잔차(오차)의 흩어진 정도를 암종별로 나눠보면:

| 암종 | 오차의 흩어짐(표준편차) |
|---|---|
| 위암 | 3.94%p |
| 유방암 | 3.96%p |
| 자궁경부암 | 4.21%p |
| 대장암 | 4.31%p |
| 간암 | 7.69%p |
| **폐암** | **14.24%p** |

폐암이 유독 크게 흔들립니다. 이유를 극단적으로 틀린 사례 10건을 직접 뜯어보니, 8건이 **85세 이상 + 폐암/간암**처럼 "그 조건에 해당하는 사람이 원래 몇 명 안 되는" 소표본 세그먼트였습니다. 예를 들어 대상자가 3명인데 3명 다 검진을 받으면 수검률이 100%가 되는데, 모델은 이걸 "이상하게 높은 값"으로 보고 33~52% 정도로만 예측해서 크게 틀립니다. 표본이 작을수록 우연히 0% 또는 100%가 나오기 쉽기 때문입니다.

이게 바로 CLAUDE.md·PRD.md에 정의된 필터가 존재하는 이유입니다.

```
MIN_RECOVERY = 300   # 대상자 300명 미만인 세그먼트는 우선순위 계산에서 제외
MAX_AGE_IDX = 11      # 75세 이상은 제외 (85세 이상이 특히 이런 소표본 노이즈의 대부분을 차지)
```

즉, **모델 자체는 전체 데이터로 학습**하지만(그래야 패턴을 폭넓게 배움), **화면에 실제로 "우선순위"로 보여줄 때는** 이렇게 신뢰하기 어려운 소표본 구간을 걸러내고 보여주는 2단계 구조입니다.

### 2-5. 무엇이 예측에 가장 큰 영향을 주나 (피처 중요도)

"이 모델이 예측할 때 어떤 정보를 가장 많이 참고하는가"를 확인하는 방법 중 하나가 **순열 중요도(Permutation Importance)**입니다. 원리는 이렇습니다.

1. 정상적으로 예측했을 때 점수(R²)를 기록
2. 특정 피처 하나(예: 소득로그)의 값을 세그먼트끼리 무작위로 뒤섞어서 일부러 "쓸모없게" 만듦
3. 다시 예측해서 점수가 얼마나 떨어지는지 확인 → **많이 떨어질수록 그 피처가 중요했다는 뜻**

실제로 돌려본 결과, 중요도 순위는:

```
연령n (압도적 1위) > 암_대장암 > 암_간암 > 여자 > 기관밀도 > 암_폐암 > ...
```

가장 하위권이 `관내0`이었고(값이 사실상 0), 이 부분은 다음 섹션에서 더 자세히 다룹니다.

---

## 3. 모델 ② 계수 해석 모델 — 자세히 뜯어보기

### 3-1. 왜 모델 ①과 별개로 하나 더 만들었나

모델 ①은 나무 300개가 겹겹이 쌓인 구조라서, "왜 이 지역이 41.87%로 예측됐는지"를 사람이 한 문장으로 설명하기 매우 어렵습니다(나무마다 서로 다른 기준으로 갈라지기 때문). 그래서 화면 문구용으로는 훨씬 단순한 **선형회귀**를 별도로 학습시킵니다.

### 3-2. 선형회귀란 — 실제 숫자로 손 계산해보기

선형회귀는 다음과 같은 아주 단순한 덧셈식입니다.

```
예측값 = 절편 + (계수1 × 여건1) + (계수2 × 여건2) + ... + (계수15 × 여건15)
```

실제로 2023년 데이터로 학습시킨 결과, 다음과 같은 절편과 계수가 나왔습니다(2026-08-19 재산출 기준, 재실행 시 값은 갱신됨).

| 여건 | 계수 | 해석 |
|---|---|---|
| 절편(intercept) | +290.65 | 모든 여건이 0일 때의 기준값 (통계적 계산값이라 실제 있을 수 없는 상황을 가정한 수치) |
| 소득로그 | −14.59 | 소득(로그 스케일)이 1 오를 때마다 수검률이 14.59%p **낮아짐** |
| 관내0 | −3.73 | 관내에 검진기관이 없으면(1) 수검률이 3.73%p 낮아짐 |
| 암_간암 | +14.27 | 간암 세그먼트는 다른 암종 평균보다 14.27%p 높은 경향 |
| 암_대장암 | −13.49 | 대장암 세그먼트는 다른 암종 평균보다 13.49%p 낮은 경향 |
| 연령n | −2.80 | 연령 구간이 1단계 올라갈 때마다 수검률이 2.80%p 낮아지는 경향 |

앞서 예로 든 제주시 세그먼트(대장암 여성 55~59세)로 실제 계산을 따라가 보면:

```
절편                    290.6479
+ 소득로그(14.26) × −14.59  =  −208.06
+ 인구로그(11.78) × −0.25   =    −2.93
+ 기관밀도(1.60) × 1.66    =    +2.66
+ 관내0(0) × −3.73         =     0.00   ← 값이 0이므로 영향 없음
+ 기관전체밀도(2.44) × −2.07 =   −5.06
+ (나머지 10개 여건도 같은 방식으로 계속 더함)
────────────────────────────
= 70.75%  (선형회귀 예측값)
```

이 70.75%는 모델 ①의 41.87%와 다릅니다 — **당연합니다.** 선형회귀는 "직선 하나"로만 설명하려다 보니 정확도는 떨어지지만(그래서 우선순위 산정에는 안 씀), 대신 "소득 항목이 −208, 관내0 항목이 0"처럼 **각 여건이 얼마씩 기여했는지 항목별로 쪼개 보여줄 수 있어** 화면 설명 문구를 만드는 데 적합합니다.

> **소득로그가 왜 이렇게 큰 마이너스일까요?** 직관과 반대로 "소득이 높을수록 국가검진 수검률(공단 통계 기준)은 낮게" 나옵니다. 소득이 높은 사람들이 검진을 안 받는 게 아니라, 회사 종합검진·민간 건강검진처럼 **국민건강보험공단 통계에 잡히지 않는 경로**로 이미 검진을 받고 있을 가능성이 높기 때문으로 추정됩니다. `coefficients.json`에 이 해석 문구가 자동으로 함께 저장됩니다.

### 3-3. 로그 스케일이 뭔가요? (소득로그, 인구로그)

소득이나 인구 같은 숫자는 값의 범위가 아주 넓습니다(소득 100만원짜리 지역과 1000만원짜리 지역이 같이 존재). 이런 데이터를 그대로 쓰면 극단적으로 큰 값 몇 개가 전체 계산을 왜곡시킵니다. **로그 변환**은 이런 큰 격차를 "압축"해서 완만하게 만들어주는 수학적 처리입니다(예: 100만원과 1000만원의 차이는 10배지만, 로그를 취하면 차이가 훨씬 작아짐). 통계 분석에서 소득·인구처럼 범위가 넓은 변수에 흔히 쓰는 표준적인 방법입니다.

### 3-4. 모델 ①과 ②가 같은 변수를 다르게 대하는 이유 (다중공선성)

앞서 요약에서 언급했듯, 모델 ①(HGB)은 `관내0`을 거의 안 쓰고, 모델 ②(선형회귀)는 이걸 핵심 근거(−3.73%p)로 씁니다. 원인은 **다중공선성**(변수끼리 정보가 겹치는 현상)일 가능성이 큽니다.

`관내0`(기관이 아예 없다=1/있다=0), `기관밀도`, `기관전체밀도`는 사실 서로 비슷한 정보를 담고 있습니다("기관이 없다"는 곧 "밀도가 0에 가깝다"는 뜻이기도 하므로). 모델 ①(나무 기반)은 여러 변수 중 아무거나 하나만 써도 같은 효과를 낼 수 있어서 셋 중 상대적으로 덜 쓰이는 `관내0`의 순열 중요도가 낮게 나올 수 있습니다. 반면 모델 ②(선형회귀)는 모든 변수에 각자의 몫을 나눠 계수를 배분하는 방식이라 `관내0`도 독립적인 계수(−3.73)를 갖습니다.

두 결과가 "모순"이라기보다, **"예측에 얼마나 필요한가"(모델 ①)**와 **"단독으로 떼어놓고 보면 얼마나 관련 있는가"(모델 ②)**라는 서로 다른 질문에 대한 답이라고 이해하면 됩니다. (이 부분은 `MODEL_VALIDATION_PLAN.md` Phase 3에서 VIF 수치로 별도 검증할 예정입니다.)

---

## 4. 모델 ③ 유사 지역 매칭(kNN) — 자세히 뜯어보기

### 4-1. "거리"를 어떻게 재나 — 좌표 위에 지역 뿌리기

전국의 모든 시군구를 5개 숫자(소득로그, 인구로그, 도시/군/구 구분, 검진기관 전체밀도)로 표현하면, 각 지역은 5차원 공간의 좌표 하나가 됩니다. 이때 두 지역이 "비슷하다"는 건 이 좌표 공간에서 **거리가 가깝다**는 뜻입니다. 사람이 3차원까지만 상상할 수 있지만, 컴퓨터는 5차원이든 100차원이든 똑같은 방식(피타고라스 정리를 확장한 유클리드 거리)으로 계산할 수 있습니다.

**표준화가 왜 필요한가** — 소득(단위: 원, 숫자가 큼)과 도시/군 구분(0 또는 1, 숫자가 작음)을 그대로 섞어서 거리를 재면 소득 차이가 결과를 다 압도해버립니다. 그래서 모든 값을 "평균 0, 표준편차 1"로 맞춰주는 표준화(StandardScaler)를 먼저 합니다 — 마치 원화와 엔화를 그냥 숫자로 비교하지 않고 환율로 환산해서 비교하는 것과 비슷합니다.

### 4-2. 실제 예시 — 서울 종로구의 유사 20곳

실제로 산출된 `similar_regions.json`을 열어보면:

```
서울특별시 종로구 → 가장 가까운 5곳:
  서울특별시 마포구, 서울특별시 영등포구, 대구광역시 수성구,
  서울특별시 광진구, 서울특별시 용산구
```

대부분 서울 안의 다른 구, 그리고 대구 수성구처럼 소득·인구밀도가 비슷한 대도시 구가 섞여 있습니다. 이 20곳의 남자 40~44세 간암 수검률을 실제로 모아보면:

| 통계 | 값 |
|---|---|
| 20곳 평균 | 75.52% |
| 최소 | 65.26% |
| 최대 | 82.29% |
| 표준오차(SE) | 1.07%p |

이게 화면에 "여건 유사 20곳 평균 75.5%"처럼 표시되는 값의 출처입니다. **표준오차가 작다는 것**은 20곳의 값이 서로 크게 다르지 않고 고르게 몰려 있다는 뜻이라 신뢰할 만하다는 뜻이고, 표준오차가 크면(2.0%p 초과) 20곳끼리도 들쭉날쭉하다는 신호라 검증 기준에서 걸러냅니다.

### 4-3. 검증 게이트 — "도농 일치율"이란

유사 20곳을 뽑았는데 만약 시골(군) 지역인데 유사 지역이 전부 대도시 구(區)로 나온다면 그건 알고리즘이 잘못 작동한 것입니다. 그래서 "기준 지역과 유사 지역의 도시/군/구 분류가 얼마나 일치하는지"를 전수 계산해서 **95% 이상**이어야 통과로 봅니다. 실제로 이 기준을 통과했는지는 `similar.py` 실행 로그에 출력됩니다.

---

## 5. 모델 ④ 층화 분석 — 자세히 뜯어보기

### 5-1. 왜 "공정한 비교"가 필요한가

만약 "검진기관이 있는 지역"과 "없는 지역"을 전국 단위로 그냥 통째로 비교하면, 애초에 검진기관이 없는 곳이 대부분 인구가 적은 농촌·저소득 지역에 몰려 있기 때문에 "기관이 없어서 낮은 건지, 원래 농촌·저소득이라 낮은 건지"를 구분할 수 없습니다. 이걸 통계에서 **교란 변수(confounding variable)** 문제라고 부릅니다.

### 5-2. 해결 방법 — 조건을 맞춘 다음 비교 (층화)

방법은 단순합니다. 도농 여부(시/군/구)와 소득 수준(상/중/하 3분위)으로 지역을 먼저 **9개의 그룹(층)**으로 나눈 뒤, **같은 그룹 안에서만** 기관 유무를 비교합니다.

```
9개 층 = (시, 군, 구) × (소득 상, 중, 하)
예: "군 지역이면서 소득 중위권"이라는 하나의 층 안에서만 비교
```

### 5-3. 실제 결과 예시

2024년 실측 층화표 상위 3개(기관 유무에 따른 차이가 가장 큰 층):

| 암종 | 층 | 기관없음 수검률 | 기관있음 수검률 | 차이 |
|---|---|---|---|---|
| 자궁경부암 | 군·소득중 | 43.56% | 56.86% | **13.30%p** |
| 폐암 | 시·소득하 | 46.65% | 59.25% | **12.60%p** |
| 대장암 | 군·소득상 | 31.39% | 41.94% | **10.55%p** |

이 표를 보면 "같은 도농·소득 조건 안에서도" 관내 검진기관 유무가 10%p 이상 차이를 만든다는 걸 확인할 수 있습니다 — 즉 단순히 지역이 가난하거나 시골이라서가 아니라, **기관 접근성 자체가 독립적인 영향**을 준다는 증거입니다. 이게 모델 ②의 계수(−3.73%p)를 뒷받침하는 별도의 근거 역할을 합니다.

> 층당 대상자가 1,000명 미만인 층은 표본이 너무 작아 신뢰하기 어려워 결과에서 제외합니다.

---

## 6. 신뢰성 관련 용어 미니 사전

| 용어 | 쉬운 설명 |
|---|---|
| R² (결정계수) | 모델의 "설명력 점수"(0~1). 1에 가까울수록 잘 맞힘 |
| MAE (평균절대오차) | 예측이 평균적으로 몇 %p씩 틀리는지 |
| 잔차 | 실제값 − 예측값. 마이너스면 예측보다 실제가 낮다는 뜻 |
| 과적합(overfitting) | 공부한 문제만 잘 풀고 새 문제는 못 푸는 상태. 학습 점수와 검증 점수 차이로 확인 |
| 교차검증(CV) | 데이터를 여러 조각으로 나눠 돌아가며 시험 봐서 점수가 우연이 아닌지 확인하는 절차 |
| 순열 중요도 | 특정 정보를 일부러 망가뜨렸을 때 성적이 얼마나 떨어지는지로 "그 정보가 얼마나 중요했는지" 재는 방법 |
| 다중공선성 | 서로 다른 두 변수가 사실상 같은 정보를 담고 있어 모델이 둘 중 하나만 골라 쓰는 현상 |
| 표준오차(SE) | 표본(예: 유사 20곳)의 값들이 평균에서 얼마나 흩어져 있는지 |
| 교란 변수 | 두 그룹을 비교할 때 진짜 원인이 아닌데 결과에 영향을 주는 숨은 제3의 변수(예: 소득) |

---

## 7. 전체 요약표

| 모델 | 파일 | 알고리즘 | 답하는 질문 | 실측 성능/기준 | 쓰이는 화면 |
|---|---|---|---|---|---|
| ① 기대치 | `analysis/expectation.py` | HistGradientBoostingRegressor | 여기, 원래 얼마나 받아야 하나? | R²=0.88, MAE=4.29%p (기준 통과) | 우선순위 |
| ② 계수 해석 | `analysis/coefficients.py` | LinearRegression | 왜 낮은가? | 정확도 기준 없음(설명용) | 요인 분석 |
| ③ 유사 지역 | `analysis/similar.py` | NearestNeighbors (kNN, k=20) | 비슷한 동네는 실제로 얼마나 하나? | 도농 일치율≥95%, SE≤2.0%p | 비교 |
| ④ 층화 분석 | `analysis/strata.py` | 그룹 집계 | 조건을 맞추면 진짜 차이가 있나? | 층당 대상자≥1,000명 | 요인 분석 보조 |

---

## 8. 참조 문서

| 문서 | 내용 |
|---|---|
| `CLAUDE.md` | 모델 관련 절대 규칙(하드코딩 금지·시간 분할 검증 등) |
| `MODEL_GUIDE.md` | 이 문서의 축약 버전(빠르게 훑어볼 때) |
| `MODEL_VALIDATION_PLAN.md` | 성능 검증 계획 및 진행 상황 |
| `PRD.md` | 화면 9종·API 명세·수용 기준 |
| `DEVELOPMENT.md` | 실행 명령·검증 게이트·트러블슈팅 |
