# MODEL_GUIDE.md — 온코케어 데이터 분석 모델 가이드 (초보자용)

> 온코케어가 화면에 보여주는 숫자가 "어디서 어떻게" 나오는지 쉬운 말로 설명한 문서.
> 코드 레벨 규칙은 `CLAUDE.md`, 상세 검증 계획은 `MODEL_VALIDATION_PLAN.md` 참고.

---

## 1. 큰 그림 — AI는 숫자를 만들지 않는다

온코케어는 화면에 나오는 숫자를 AI(LLM)가 그 자리에서 즉석으로 만들지 않는다. 대신 4가지 통계·머신러닝 분석을 미리 계산해 파일로 저장해두고(`data/processed/`), 사용자가 화면을 열면 그 저장된 결과를 그대로 읽어서 보여준다. AI는 나중에 **이미 계산된 숫자를 문장으로 풀어 쓰는 역할만** 한다.

```
원본 공공데이터 (건강보험공단 · 국민연금공단 등)
        ↓
89,000여 개의 "세그먼트" (지역 × 성별 × 연령 × 암종 조합)
        ↓
   ┌────┴────┬────────┬────────┐
 모델①      모델②     모델③      모델④
 기대치      계수해석    유사지역     층화분석
        ↓
   화면에 표시 (숫자는 항상 여기서만 나온다)
```

**세그먼트**란 데이터를 잘게 쪼갠 한 조각이다. 예: *"서울 양천구 · 여자 · 60~64세 · 위암"*. 이런 조각이 8만9천 개 넘게 있고, 조각마다 "검진 대상자 수"와 "실제 검진받은 사람 수"가 기록돼 있다.

---

## 2. 모델 ① 기대치 모델 — "이 지역은 원래 이 정도는 나와야 하는데?"

**비유** — 반 평균이 아니라, 나와 비슷한 조건(공부 시간·학원 유무 등)의 학생들이 보통 몇 점 받는지를 예측하는 것.

| 항목 | 내용 |
|---|---|
| 파일 | `analysis/expectation.py` |
| 알고리즘 | `HistGradientBoostingRegressor` (사이킷런 기본 포함) |
| 입력(15개 여건 피처) | 소득 수준·인구 규모·검진기관 밀집도·관내 기관 유무·도시/농촌/일반구 여부·연령대·성별·암종(6종) |
| 출력 | 기대치(이 조건이면 정상적으로 나와야 할 수검률) |
| 파생값 | `잔차 = 실제 수검률 − 기대치`, `회복여지 = 잔차 × 대상자 ÷ 100` |
| 학습/검증 | 2023년 데이터로 학습, 2024년 데이터로 검증(무작위 분할 아님) |
| 쓰이는 화면 | 우선순위 화면 — 회복 여지가 큰 지역·암종부터 정렬 |

**왜 이 알고리즘인가** — 여러 개의 "간단한 규칙 나무"를 수백 개 이어 붙여 오차를 계속 줄여가는 방식이다(엑셀 IF문 수백 개를 겹쳐 쓴다고 생각하면 비슷하다). 단순 직선(선형회귀)보다 "소득도 낮고 인구도 적으면 더 크게 떨어진다"처럼 조건이 겹칠 때의 패턴을 잘 잡는다.

**검증 결과 (2026-08-19 실측, 재실행 시 재검증)**

| 지표 | 값 | 기준 |
|---|---|---|
| Test R² | 0.8809 | ≥ 0.85 |
| Test MAE | 4.29%p | ≤ 4.5%p |

> **주의할 점** — 폐암, 그중에서도 85세 이상처럼 표본이 아주 적은 세그먼트에서 유독 잘 틀린다(잔차 표준편차가 다른 암종의 2~4배). 그래서 실제 서비스에서는 대상자 300명 미만, 75세 이상 세그먼트를 우선순위 계산에서 제외하는 필터를 둔다.

---

## 3. 모델 ② 계수 해석 모델 — "무엇이 수검률을 얼마나 움직이는가?"

**비유** — "출석을 1번 빼먹으면 성적이 평균 2점 떨어진다"처럼, 요인 하나하나의 영향력을 딱 떨어지는 숫자로 알려주는 표.

| 항목 | 내용 |
|---|---|
| 파일 | `analysis/coefficients.py` |
| 알고리즘 | `LinearRegression` (선형회귀) |
| 입력 | 모델 ①과 동일한 15개 여건 피처 |
| 출력 | 각 피처의 계수(1 늘어나면 결과가 몇 %p 변하는지) + 해석 문구 |
| 쓰이는 화면 | 요인 분석 화면 — "우리 지역이 낮은 이유는 소득 때문인가, 검진기관 접근성 때문인가" 설명 |

모델 ①(HGB)은 예측은 잘하지만 나무 수백 개가 겹쳐 있어서 "왜 그렇게 예측했는지"를 사람이 읽기 쉬운 한 문장으로 설명하기 어렵다. 그래서 별도로 더 단순한 선형회귀를 하나 더 돌린다. 예측 정확도는 모델 ①보다 떨어지지만, 대신 "왜"를 설명하는 문장을 만드는 데 쓴다.

> **주의할 점** — 모델 ①은 실제 검증에서 "관내 검진기관 유무" 변수를 거의 안 쓰는 것으로 나타났다(다른 변수와 정보가 겹쳐서, permutation importance ≈ 0). 반면 모델 ②는 이 변수를 화면 문구의 핵심 근거로 쓴다. 두 모델이 서로 다른 목적(예측 vs 설명)을 갖고 있어 생기는 차이이며, 화면에 두 결과를 함께 노출할 때는 혼동되지 않도록 설명이 필요하다. (`MODEL_VALIDATION_PLAN.md` Phase 3에서 다중공선성 여부를 별도 조사 예정)

---

## 4. 모델 ③ 유사 지역 매칭 (kNN) — "나랑 비슷한 동네는 실제로 몇 % 받고 있나?"

**비유** — 키·몸무게·나이가 비슷한 친구들의 평균 기록과 내 기록을 비교하는 것. 모델 ①이 "수식으로 계산한 기대치"라면, 모델 ③은 "실제로 존재하는 비슷한 동네들의 진짜 평균"이라 더 직관적이다.

| 항목 | 내용 |
|---|---|
| 파일 | `analysis/similar.py` |
| 알고리즘 | `NearestNeighbors` (k-최근접 이웃, k=20) |
| 입력 | 소득·인구·도농 여부·검진기관 밀집도로 좌표화한 시군구 프로필 |
| 출력 | 거리가 가장 가까운 20개 시군구, 그 20곳의 실제 수검률 평균·최소·최대·표준오차 |
| 검증 기준 | 도농 일치율 ≥ 95% · 평균 표준오차 ≤ 2.0%p |
| 쓰이는 화면 | 비교 화면 — "우리 지역 vs 여건 유사 20곳" |

**왜 이게 중요한가** — 화면에 "목표 51%"라고만 쓰면 AI가 임의로 정한 목표처럼 보인다. "여건이 비슷한 20곳이 실제로 51%를 달성했다"라고 쓰면 실현 가능하다는 근거가 된다. 이것이 온코케어가 화면 표현 규칙(`CLAUDE.md` 7번 규칙)으로 못박은 이유다.

---

## 5. 모델 ④ 층화 분석 — "도시·농촌 × 소득 상/중/하로 나눠보면?"

**비유** — 전교 등수 대신 "저소득층 학생들끼리만 비교했을 때" 등수를 매기는 것. 이렇게 하면 원래 조건이 불리한 그룹을 뭉뚱그려 억울하게 낮은 평가를 받는 걸 막을 수 있다.

| 항목 | 내용 |
|---|---|
| 파일 | `analysis/strata.py` |
| 방식 | 머신러닝이 아니라 그룹별 집계(평균 계산) |
| 분류 기준 | 도농(시/군/구) 3종류 × 소득 3분위(상/중/하) = **9개 층** |
| 출력 | 같은 층 안에서 "관내 검진기관 있음" vs "없음"의 수검률 차이 |
| 쓰이는 화면 | 조건이 비슷한 그룹 안에서 검진기관 유무가 실제로 얼마나 차이를 만드는지 근거 제시 |

---

## 6. 보조 집계 2종 (모델 아님)

| 이름 | 파일 | 하는 일 |
|---|---|---|
| 전국 평균 | `analysis/build_artifacts.py`(`run_national_avg`) | 세그먼트별 전국 수검률 단순 평균 — 전국 대비 격차 비교용 |
| 연도별 추이 | `analysis/trend.py` | 같은 지역·암종의 연도별 수검률 변화 — 급변 탐지용 |

단순히 "더하고 나누기"라서 정확도 검증 대상이 아니다.

---

## 7. 왜 하나가 아니라 4개로 나눴나

각 모델이 답하는 질문이 서로 다르기 때문이다.

| 질문 | 담당 모델 |
|---|---|
| 이 지역, 원래 이 정도는 나와야 하는데 실제로는? (예측) | ① 기대치 |
| 왜 그런가, 무엇 때문인가? (설명) | ② 계수 |
| 비슷한 동네는 실제로 얼마나 하고 있나? (실증) | ③ 유사지역 |
| 조건을 통제하면 검진기관 유무가 진짜 영향이 있나? (검정) | ④ 층화 |

하나의 모델로 이 4가지를 다 잘하려 하면 정확도와 설명력이 서로 상충한다(정확할수록 설명하기 어려워진다). 그래서 목적별로 나눴고, 이것이 모델 ①과 ②가 같은 변수(관내0)를 다르게 취급하는 근본 이유이기도 하다 — 애초에 서로 다른 일을 하도록 설계됐기 때문이다.

---

## 8. 전체 요약표

| 모델 | 파일 | 알고리즘 | 답하는 질문 | 쓰이는 화면 |
|---|---|---|---|---|
| ① 기대치 | `analysis/expectation.py` | HistGradientBoostingRegressor | 여기, 원래 얼마나 받아야 하나? | 우선순위 |
| ② 계수 해석 | `analysis/coefficients.py` | LinearRegression | 왜 낮은가? | 요인 분석 |
| ③ 유사 지역 | `analysis/similar.py` | NearestNeighbors (kNN) | 비슷한 동네는 실제로 얼마나 하나? | 비교 |
| ④ 층화 분석 | `analysis/strata.py` | 그룹 집계 | 조건을 맞추면 진짜 차이가 있나? | 요인 분석 보조 |

---

## 9. 참조 문서

| 문서 | 내용 |
|---|---|
| `CLAUDE.md` | 모델 관련 절대 규칙(하드코딩 금지·시간 분할 검증 등) |
| `MODEL_VALIDATION_PLAN.md` | 성능 검증 계획 및 진행 상황 |
| `PRD.md` | 화면 9종·API 명세·수용 기준 |
| `DEVELOPMENT.md` | 실행 명령·검증 게이트·트러블슈팅 |
